---
plan_id: 03-01
phase: 03
wave: 1
depends_on: []
files_modified:
  - app/api/model.py
  - app/api/__init__.py
  - app/route/user.py
autonomous: true
requirements:
  - COMMENT-01
  - COMMENT-02
  - COMMENT-03
  - COMMENT-06
---

# Plan 03-01: Comment 数据模型重构 + 发布/列表接口

<objective>
将 `Comment` 模型从已注释的"半成品"重构为生产可用:删 `user_name` 字符串列、改用 `user_id` 外键关联 `User`、补 `Lemma.comments` 级联、修 `Comment.__init__/__str__` 残留 bug;并实装 `POST /api/comment` 发布端点、`POST /api/comment/<id>/delete` 作者删除端点(走 `api` 蓝图);`/user/detail` 路由补 `comments` 查询,按时间倒序传给模板。`detail.html` / `app/api/admin.py` 不动(归 Plan 3.2)。
</objective>

<must_haves>
- [ ] SC-1: 登录用户从 detail 提交 form (action=/api/comment) → 服务端 302 → 详情页(整页刷新,新评论显示在列表顶部)
- [ ] SC-2: 匿名 POST /api/comment 被 `@login_required` 拦截,走 Phase 2 D-17 统一 401/403 错误页
- [ ] SC-3: 详情页评论按 `Comment.time DESC` 倒序(最新评论在列表顶部)
- [ ] SC-6: `Comment.user_id` FK → `User.id` 强约束(`nullable=False`);模板通过 `comment.author.name` 渲染当前用户名(rename 改字段不孤历史评论)
</must_haves>

<tasks>
<task type="auto">
  <name>Task 1: Comment schema 改造 (User / Lemma / Comment 三处)</name>
  <read_first>
    - app/api/model.py
    - .planning/phases/03-comment-system/03-CONTEXT.md (D-26, D-32, D-33, D-34)
    - .planning/phases/03-comment-system/03-PATTERNS.md (model.py 节)
  </read_first>
  <action>
    按 D-26/D-32/D-33/D-34 改写 `app/api/model.py`:

    1. **新增 import** (在 `from sqlalchemy.orm import ...` 行,或在 `from flask_sqlalchemy import SQLAlchemy` 同一区域追加):
       `from sqlalchemy.orm import backref`

    2. **User 类** (L9-23):在 `is_admin` 字段(L15)之后追加关系属性:
       ```python
       comments = db.relationship(
           'Comment',
           backref=backref('author', lazy='joined'),
           cascade='all, delete-orphan',
       )
       ```

    3. **Lemma 类** (L25-38):L31 一行改为多行:
       ```python
       comments = db.relationship(
           'Comment',
           backref='lemmas',
           lazy='dynamic',
           cascade='all, delete-orphan',
       )
       ```

    4. **Comment 类** (L40-58) **整段重写**为:
       ```python
       class Comment(db.Model):
           __tablename__ = 'comment'
           id = db.Column(db.Integer, primary_key=True)
           user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)
           lemma_id = db.Column(db.Integer, db.ForeignKey('lemma.id'), nullable=False)
           content = db.Column(db.String(320))
           time = db.Column(db.DateTime, default=datetime.datetime.utcnow)

           def __str__(self):
               snippet = (self.content or '')[:20]
               return '评论<%s>' % (snippet)

           def __init__(self, user_id=None, lemma_id=None, content=None):
               self.user_id = user_id
               self.lemma_id = lemma_id
               self.content = content
       ```
       关键点:删 L45 `user_name = db.Column(...)`;加 L46 风格 `user_id` FK (`nullable=False`);L46 改 `nullable=False`;L49 `time` 列 `default=datetime.datetime.utcnow` 保留(不删);L51-52 `__str__` 不再访问不存在的 `self.title`,改用 `self.content` 前 20 字;L54-58 `__init__` 改 `user_id=...` / `lemma_id=...` 形参,删 `self.user_name = current_user` (赋 User 对象到字符串列,TypeError),删 `self.lemma_title = ...`,**不**写 `self.time`(否则覆盖 column default,所有评论时间戳同秒)。

    5. **`init_db()` 函数体 (L61-76) 不动** — Phase 2 D-02 既有 `db.drop_all() + db.create_all() + 灌种子` 自动覆盖新加的 `user_id` 列(种子没有评论,D-26 决定不留旧数据)。**不**修改 `init_db()` 函数体。

    6. 保留 L1-7 的 imports(`flask_sqlalchemy`, `flask_login`, `werkzeug.security`, `datetime`, `db = SQLAlchemy()`)。`current_user` 在 `flask_login` import 行保留(虽然 `__init__` 不再使用,但 Phase 2 注册/登录/其他视图仍用,不要删 import)。
  </action>
  <acceptance_criteria>
    - `app/api/model.py` 中 `grep -F "user_name = db.Column" app/api/model.py` 退出码 1 (无匹配)
    - `app/api/model.py:User` 含 `comments = db.relationship('Comment', backref=backref('author', lazy='joined'), cascade='all, delete-orphan')`
    - `app/api/model.py:Comment` 含 `user_id = db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)`
    - `app/api/model.py:Comment` 含 `lemma_id = db.Column(db.Integer, db.ForeignKey('lemma.id'), nullable=False)`
    - `app/api/model.py:Comment` 含 `content = db.Column(db.String(320))`
    - `app/api/model.py:Comment` 含 `time = db.Column(db.DateTime, default=datetime.datetime.utcnow)`
    - `app/api/model.py:Comment.__init__` 不含 `self.user_name = current_user` (grep -F 'self.user_name = current_user' 退出 1)
    - `app/api/model.py:Comment.__init__` 不含 `self.time = datetime.now()` (grep -F 'self.time = datetime.now()' 退出 1)
    - `app/api/model.py:Comment.__init__` 不含 `self.lemma_title` (grep -F 'self.lemma_title' 退出 1)
    - `app/api/model.py:Comment.__str__` 含 `(self.content or '')[:20]`
    - `app/api/model.py:Lemma.comments` 含 `cascade='all, delete-orphan'`
    - `app/api/model.py` 含 `from sqlalchemy.orm import backref`
    - `python -c "from app.api.model import Comment, User, Lemma, db; print(Comment, User, Lemma, db)"` 退出码 0
    - `flask init-db` 退出码 0 (drop_all + create_all + seed 1 admin user 'a' + 7 lemmas)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 实装 POST /api/comment 端点 (解注释 + 修 typo + 业务校验)</name>
  <read_first>
    - app/api/__init__.py
    - app/api/model.py (改后的 Comment / Lemma)
    - .planning/phases/03-comment-system/03-CONTEXT.md (D-35, CD-06, CD-07)
    - .planning/phases/03-comment-system/03-PATTERNS.md (__init__.py 节)
    - .planning/phases/02-security-auth-hardening/02-PATTERNS.md (`@login_required` 模式)
  </read_first>
  <action>
    在 `app/api/__init__.py` 顶部 imports (L2-5) 已有 `redirect`, `url_for`, `flash`, `request`, `current_user`, `Comment`, `Lemma`, `db`, 无需新增。

    1. **删除** L82-91 的注释块(被注释的旧 `/commen` 路由):
       ```python
       # @api.route('/commen', methods=['POST'])
       # @login_required
       # def commen():
       #     content = request.form.get('commentcontent')
       #     user_name = current_user
       #     lemma_id =
       #     comment = Comment(lemma_id=lemma_id, user_name=user_name, content=content)
       #     db.session.add(comment)
       #     db.session.commit()
       #     return flash('评论发表成功')
       ```

    2. **在 `modify` 路由 (L67-79) 之后、`reset` 路由 (L93) 之前** 新增实装的 `POST /api/comment` 端点:
       ```python
       @api.route('/comment', methods=['POST'])
       @login_required
       def comment():
           lemma_id = request.form.get('lemma_id')
           content = (request.form.get('content') or '').strip()
           # CD-06: 1-320 字符校验,空内容(纯空格)拒收
           if not content or len(content) > 320:
               flash('评论内容不能为空且不超过 320 字符')
               return redirect(request.referrer or url_for('apple.home'))
           # CD-07: lemma_id 不存在 → flash 词条不存在 + 302 回 home
           lemma = Lemma.query.get(lemma_id)
           if lemma is None:
               flash('词条不存在')
               return redirect(url_for('apple.home'))
           new_comment = Comment(
               user_id=current_user.id,
               lemma_id=lemma_id,
               content=content,
           )
           db.session.add(new_comment)
           db.session.commit()
           flash('评论发表成功')
           return redirect(request.referrer or url_for('apple.home'))
       ```

       关键修正:
       - 路径从 `/commen` (typo) 改为 `/comment`
       - 函数名从 `commen` 改为 `comment`
       - 入参 `commentcontent` → `content` (对齐 detail.html 内联 form 的 `<textarea name="content">`,由 Plan 3.2 渲染)
       - 删 `user_name = current_user` 反模式,改用 `user_id=current_user.id` (D-26 schema)
       - 删 `lemma_id =` (语法错误占位符),从 `request.form.get('lemma_id')` 取
       - 删 `self.time = datetime.now()` 写法,模型层 column default 自动填充
       - `request.referrer or url_for('apple.home')` 兜底回 home,Phase 3 整页刷新,detail 页由 `/user/detail` (POST) 渲染,referrer 即可
  </action>
  <acceptance_criteria>
    - `app/api/__init__.py` 不含 `# @api.route('/commen'` 开头的注释行 (grep -F "# @api.route('/commen'" 退出 1)
    - `app/api/__init__.py` 含 `@api.route('/comment', methods=['POST'])` (非注释行)
    - `app/api/__init__.py` 含 `def comment():` 函数定义(非注释)
    - `app/api/__init__.py` 含 `@login_required` 在 `def comment():` 之上
    - `app/api/__init__.py:comment` 含 `Comment(user_id=current_user.id, lemma_id=lemma_id, content=content)` (非注释)
    - `app/api/__init__.py:comment` 含 `Lemma.query.get(lemma_id)` 校验
    - `app/api/__init__.py:comment` 含 `flash('评论内容不能为空且不超过 320 字符')`
    - `app/api/__init__.py:comment` 含 `flash('词条不存在')`
    - `app/api/__init__.py:comment` 含 `flash('评论发表成功')`
    - `python -c "from app.api import api; print([str(r) for r in api.deferred_functions])" 2>/dev/null; python -c "from app import create_app; a = create_app(); print([r.rule for r in a.url_map.iter_rules() if 'comment' in r.rule])"` 退出码 0 且输出含 `/api/comment`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 实装 POST /api/comment/<id>/delete 作者删除端点</name>
  <read_first>
    - app/api/__init__.py
    - .planning/phases/03-comment-system/03-CONTEXT.md (D-27 修正版, D-36, CD-08)
    - .planning/phases/03-comment-system/03-PATTERNS.md (__init__.py 节)
  </read_first>
  <action>
    在 `app/api/__init__.py` 的 `comment` 端点 (Task 2 新增) 之后、`reset` 路由 (L93) 之前新增作者删除端点。**不**带 `@admin_required`(admin 删评论走 Plan 3.2 的 `admin` 蓝图,D-27 修正)。

    ```python
    @api.route('/comment/<int:comment_id>/delete', methods=['POST'])
    @login_required
    def delete_comment(comment_id):
        comment = Comment.query.get(comment_id)
        if comment is None:
            flash('评论不存在')
            return redirect(request.referrer or url_for('apple.home'))
        # D-27 修正:仅作者本人可删;非作者 → 403 走 Phase 2 D-17 统一错误页
        if comment.user_id != current_user.id:
            abort(403)
        db.session.delete(comment)
        db.session.commit()
        flash('评论已删除')
        return redirect(request.referrer or url_for('apple.home'))
    ```

    关键点:
    - `@login_required` 已从 `flask_login` import 到位(L3)
    - `abort` 已从 `flask` import 到位(L2)
    - 函数内 `user_id != current_user.id` 双保险,即使装饰器被绕过也守住作者权限
    - `request.referrer` 兜底 `url_for('apple.home')` (CD-08 推荐 referrer)
  </action>
  <acceptance_criteria>
    - `app/api/__init__.py` 含 `@api.route('/comment/<int:comment_id>/delete', methods=['POST'])` (非注释行)
    - `app/api/__init__.py` 含 `def delete_comment(comment_id):` 函数定义
    - `app/api/__init__.py:delete_comment` 含 `@login_required` 装饰器
    - `app/api/__init__.py:delete_comment` 含 `abort(403)`
    - `app/api/__init__.py:delete_comment` 含 `comment.user_id != current_user.id` 校验
    - `app/api/__init__.py:delete_comment` 含 `db.session.delete(comment)` 与 `db.session.commit()`
    - `app/api/__init__.py:delete_comment` 含 `flash('评论已删除')`
    - `app/api/__init__.py:delete_comment` **不**含 `@admin_required` 装饰器
    - `python -c "from app import create_app; a = create_app(); print([r.rule for r in a.url_map.iter_rules() if 'comment' in r.rule])"` 退出码 0 且输出同时含 `/api/comment` 与 `/api/comment/<int:comment_id>/delete`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: /user/detail 路由补 comments 查询 + 倒序</name>
  <read_first>
    - app/route/user.py
    - app/api/model.py (改后的 Comment 含 user_id)
    - .planning/phases/03-comment-system/03-CONTEXT.md (D-39, D-40)
    - .planning/phases/03-comment-system/03-PATTERNS.md (user.py 节)
  </read_first>
  <action>
    改 `app/route/user.py` 的 `detail` 路由函数体 (L39-46):

    现有:
    ```python
    @user.route('/detail', methods=['POST'])
    def detail():
        entirelytitle = request.form.get('linklist')
        fullcontent = Lemma.query.filter_by(title = entirelytitle).all()
        if not fullcontent:
            flash('所查词条不存在')
            return redirect(url_for('apple.home'))
        return render_template('detail.html', fullcontent=fullcontent)
    ```

    改为(D-39 补 comments 查询,D-40 按 time DESC 倒序,CD-05 N+1 由 User 关系 lazy='joined' 自动防):
    ```python
    @user.route('/detail', methods=['POST'])
    def detail():
        entirelytitle = request.form.get('linklist')
        fullcontent = Lemma.query.filter_by(title = entirelytitle).all()
        if not fullcontent:
            flash('所查词条不存在')
            return redirect(url_for('apple.home'))
        # D-39: 传 comments 给模板;按时间倒序(最新评论在前)
        comments = Comment.query.filter_by(lemma_id=fullcontent[0].id).order_by(Comment.time.desc()).all()
        return render_template('detail.html', fullcontent=fullcontent, comments=comments)
    ```

    关键点:
    - `Comment` 已从 `app.api.model` import (L4),无需新增 import
    - `fullcontent[0].id` 沿用既有写法(Phase 1 修复 BaseQuery bug 后,`fullcontent` 已是 list)
    - `Comment.time.desc()` 倒序;Phase 3 模板侧 `comment.time.strftime('%Y-%m-%d %H:%M')` 在 Plan 3.2 任务里执行
  </action>
  <acceptance_criteria>
    - `app/route/user.py:detail` 含 `comments = Comment.query.filter_by(lemma_id=fullcontent[0].id).order_by(Comment.time.desc()).all()` (非注释)
    - `app/route/user.py:detail` 末行含 `render_template('detail.html', fullcontent=fullcontent, comments=comments)` (非注释)
    - `app/route/user.py:detail` 末行不含 `render_template('detail.html', fullcontent=fullcontent)` (缺 comments 参数) — grep -F 'render_template..detail.html., fullcontent=fullcontent)' 退出 1
    - `python -c "from app import create_app; a = create_app(); print([r.rule for r in a.url_map.iter_rules() if 'detail' in r.rule])"` 退出码 0 且输出含 `/user/detail`
  </acceptance_criteria>
</task>
</tasks>

<verification>
完成 Plan 3.1 后,做以下端到端 smoke 验证(覆盖 SC-1/SC-2/SC-3/SC-6):

1. **DB schema 验证**:
   - `flask init-db` 退出码 0
   - `python -c "from app import create_app; from app.api.model import db, Comment; a = create_app(); ctx = a.app_context(); ctx.push(); print(Comment.__table__.columns.keys())"` 退出码 0,输出含 `user_id` 与 `lemma_id`

2. **URL map 验证**:
   - `python -c "from app import create_app; a = create_app(); print(sorted([r.rule + ' ' + ','.join(r.methods - {'HEAD','OPTIONS'}) for r in a.url_map.iter_rules() if 'comment' in r.rule]))"` 退出码 0,输出含 `/api/comment POST` 与 `/api/comment/<int:comment_id>/delete POST`

3. **匿名 POST 拦截** (SC-2):
   - `curl -s -o /dev/null -w "%{http_code}" -X POST http://127.0.0.1:5000/api/comment -d "lemma_id=1&content=hi"` 在未登录时应返回 401/403

4. **登录用户发布评论** (SC-1):
   - 登录 admin `a/a` → POST /api/comment with lemma_id=1&content=测试评论 → 302 跳转 → /user/detail 列表顶部出现 "测试评论" 与 `comment.author.name == "a"`

5. **作者删除评论** (SC-1/SC-7):
   - 登录 `a` → POST /api/comment/1/delete → 302 → 详情页评论列表少一条
   - 登录另一用户(非 a) → POST /api/comment/<a 的评论 id>/delete → 403

6. **倒序展示** (SC-3):
   - 同 lemma 发两条评论,刷新详情页,新评论在上

7. **rename 不孤** (SC-6):
   - 假设 admin 把 `a` 改名为 `a-renamed`(本阶段不实装改名功能,直接 SQL `UPDATE user SET name='a-renamed' WHERE id=1`) → 详情页评论 `{{ comment.author.name }}` 显示 `a-renamed` 而非 `a`,且评论行数不变
</verification>

<artifacts_this_phase_produces>
- `app/api/model.py:User.comments` relationship (with `backref=backref('author', lazy='joined'), cascade='all, delete-orphan'`)
- `app/api/model.py:Comment.user_id` column (`db.Column(db.Integer, db.ForeignKey('user.id'), nullable=False)`)
- `app/api/model.py:Comment.lemma_id` column (changed to `nullable=False`)
- `app/api/model.py:Comment.__init__` rewritten (takes `user_id`/`lemma_id`/`content`; no `self.user_name`/`self.lemma_title`/`self.time` writes)
- `app/api/model.py:Comment.__str__` rewritten (returns snippet of `self.content`[:20])
- `app/api/model.py:Lemma.comments` relationship (adds `cascade='all, delete-orphan'`)
- `app/api/model.py` import: `from sqlalchemy.orm import backref`
- `app/api/__init__.py:comment` route (`POST /api/comment`, `@login_required`)
- `app/api/__init__.py:delete_comment` route (`POST /api/comment/<int:comment_id>/delete`, `@login_required` + author check)
- `app/route/user.py:detail` route body: queries `Comment.query.filter_by(lemma_id=...).order_by(Comment.time.desc()).all()`, passes `comments=comments` to template
- URL rules registered: `/api/comment POST`, `/api/comment/<int:comment_id>/delete POST`
- DB table `comment` (recreated by `flask init-db`): columns `id`, `user_id` (FK user.id, NOT NULL), `lemma_id` (FK lemma.id, NOT NULL), `content VARCHAR(320)`, `time DATETIME`
</artifacts_this_phase_produces>
