---
plan_id: 04-03
phase: 04
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/api/model.py
  - app/api/__init__.py
  - app/route/user.py
  - app/templates/_comment.html (verify exists from 04-01)
  - app/templates/base.html (verify from 04-01)
  - app/templates/detail.html (verify from 04-02)
autonomous: true
requirements:
  - LEMMA-01
  - LEMMA-02
  - LEMMA-04
  - LEMMA-05
  - LEMMA-06
  - LEMMA-07
  - LEMMA-08
  - FRONT-06
---

# Plan 04-03: 词条产品特性(updated_at / view_count / wiki 链接 / 相关词条 / HTMX 登录态刷新)

<objective>
在 Plan 4.1 + 4.2 完成的 Pico.css + HTMX + Quill 基础上,补齐词条产品特性:`Lemma` 模型加 `updated_at` + `view_count` 两列(D-61);`/user/detail` 改 GET `?title=...` + view_count 原子 +1(D-48 / D-65);注册 Jinja2 `wikilink` filter 解析 `[[词条名]]` → 蓝链接 / 红色虚线 + "(创建此词条)" (D-50 / D-49);`/user/detail` 查 backlinks(`Lemma.content.contains('[[' + title + ']]')`)(D-69)传给 detail.html "相关词条" 区(D-68);`/api/login` 和 `/api/logout` 加 `HX-Trigger: nav-refresh` 响应头(D-47);新增 `/api/nav-fragment` GET 端点供 base.html 导航栏 HTMX 监听(D-47);`/api/comment` 检测 `HX-Request` 走 HTMX swap 返 `_comment.html` 片段(D-46 / D-73)。

本 plan 是 Wave 2,与 Plan 4.2 并行;依赖 04-01 的 base.html(已含 nav-refresh HTMX 监听 + 暗色切换)。
</objective>

<must_haves>
- [ ] LEMMA-01 (本 plan 不直接交付,但 LEMMA-04 配套): Lemma 有 `updated_at` + `view_count` 字段,自动初始化
- [ ] LEMMA-02 (本 plan 不直接交付,Phase 1 已修 /api/modify bug): 配合 Plan 4.1 的 bleach 过滤,modify 提交流程完整
- [ ] LEMMA-04: detail.html 渲染完整 lemma(标题 + 内容 + 最后编辑时间 + 浏览数 + 评论 + 相关词条)
- [ ] LEMMA-05: `/user/detail` GET 进入时,`view_count` 走 SQLAlchemy 2.x `update(Lemma).where(Lemma.id == lemma.id).values(view_count=Lemma.view_count + 1)` 原子 +1,避免 race condition
- [ ] LEMMA-06: detail.html 显示 "最后编辑于 YYYY-MM-DD HH:MM"(`{{ fullcon.updated_at.strftime('%Y-%m-%d %H:%M') }}`),作者字段(可选,Plan 4.2 已留占位)
- [ ] LEMMA-07: Jinja2 `wikilink` filter 解析 `[[xxx]]` — 存在 → 蓝链接 `<a href="/user/detail?title=xxx">xxx</a>`,不存在 → 红色虚线 `<a class="wikilink-missing" style="text-decoration:underline dashed red;color:var(--pico-color-red-500)">xxx (创建此词条)→</a>`(跳 `/user/add?title=xxx`)
- [ ] LEMMA-08: detail.html `<aside>` "相关词条" 列出 backlinks(`Lemma.query.filter(Lemma.content.contains('[[' + fullcon.title + ']]')).limit(10).all()`),空时不渲染
- [ ] FRONT-06: `[[标题]]` 在写时(bleach)和读时(wikilink filter)双重处理 — 写时 bleach 不删 `[[` 和 `]]`(它们不是 HTML 标签),读时 wikilink 解析为 `<a>` 标签
- [ ] D-47: `/api/login` 成功 → 返 `HX-Trigger: nav-refresh` header;`/api/logout` 成功 → 返 `HX-Trigger: nav-refresh` header;`/api/nav-fragment` GET 返 navbar 右侧 nav-right 片段(登录态感知)
- [ ] D-46: `/api/comment` 检测 `request.headers.get('HX-Request')` — 存在时返 `_comment.html` 片段(HTTP 200),不存在时 flash + 302 redirect(向后兼容 Phase 3 整页)
- [ ] 7 子模板(base.html detail.html)继承关系不破坏
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Lemma 模型加 updated_at + view_count 列(D-61 / D-62 / D-67)</name>
  <read_first>
    - `app/api/model.py`(L31-49 `Lemma` 模型当前实现)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-61, D-62, D-63, D-64, D-67)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-64 init_db() 自动覆盖)
    - .planning/codebase/STACK.md (SQLAlchemy 2.x update API)
  </read_first>
  <action>
    **改** `app/api/model.py` `Lemma` 类(L31-49):

    1. **加两列**(在现有 `id / title / content` 列后,`comments` 关系前):
       ```python
       updated_at = db.Column(db.DateTime, default=datetime.datetime.utcnow, onupdate=datetime.datetime.utcnow, nullable=False)
       view_count = db.Column(db.Integer, default=0, nullable=False, server_default='0')
       ```

    2. **`__init__` 不显式赋值**(D-67):
       - 现有 `__init__(self, title=None, content=None)` 保持不变
       - `view_count` 走 column default + server_default 双兜底
       - `updated_at` 走 column default

    关键点:
    - `server_default='0'` — MySQL 列默认值(双保险,即使 Lemma() 不传参也能落库,D-61 决策)
    - `onupdate=datetime.datetime.utcnow` — SQLAlchemy 在 UPDATE 时自动刷新字段(D-62 决策,词条创建/修改时更新,评论发布/删除不触发)
    - `nullable=False` — 不允许空时间戳
    - `default=datetime.datetime.utcnow` — 新建时初始值
    - **不**改 `__tablename__`(已有 `'lemma'`)
    - **不**改 `comments` 关系(Phase 3 D-33 已就绪)
    - **不**改 `__str__` / `__repr__`(避免不必要修改,CLAUDE.md 原则 3:不"改善"无关代码)

    **init_db() 不改**(D-64):`init_db()` 函数体(L70-85)已经是 `drop_all + create_all + 灌种子`,新列自动随 `create_all()` 创建。**不**需新增 import。

    验证:`python -c "from app.api.model import Lemma; print([c.name for c in Lemma.__table__.columns])"` 退出 0,输出列表含 `id` `title` `content` `updated_at` `view_count`。
  </action>
  <acceptance_criteria>
    - `app/api/model.py` 含 `updated_at = db.Column(db.DateTime, default=datetime.datetime.utcnow, onupdate=datetime.datetime.utcnow, nullable=False)`
    - `app/api/model.py` 含 `view_count = db.Column(db.Integer, default=0, nullable=False, server_default='0')`
    - `app/api/model.py:Lemma` 类的 `__init__` 函数体不显式赋值 `view_count=0` 或 `updated_at=...`(D-67,走 column default)
    - `app/api/model.py:init_db` 函数体未改(D-64:不引入 alembic,走 drop+create 重灌)
    - `python -c "from app.api.model import Lemma; print([c.name for c in Lemma.__table__.columns])"` 退出 0,输出含 `'updated_at'` 和 `'view_count'`
    - `app/api/model.py` import 块未改(`datetime` 已有)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: /user/detail 改 POST → GET + view_count 原子 +1(D-48 / D-65)</name>
  <read_first>
    - `app/route/user.py`(L39-48 `/user/detail` 当前 POST 实现)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-48, D-65, D-66)
    - .planning/codebase/STACK.md (SQLAlchemy 2.x `update().where().values()` API)
    - 改写后的 `app/templates/detail.html`(Plan 4.2 Task 7,确认 `fullcon` 单对象用法)
  </read_first>
  <action>
    **改** `app/route/user.py` `/user/detail` 路由(L39-48)与 import 块:

    1. **顶部 import 块**(现有 L1-5):在 `from app.api.model import User, Lemma, Comment, db` 后加一行:
       ```python
       from sqlalchemy import update
       ```

    2. **改 `/user/detail` 路由**(L39-48):
       ```python
       @user.route('/detail', methods=['GET'])
       def detail():
           title = request.args.get('title', '').strip()
           if not title:
               flash('未指定词条')
               return redirect(url_for('apple.home'))
           fullcon = Lemma.query.filter_by(title=title).first()
           if fullcon is None:
               flash('所查词条不存在')
               return redirect(url_for('apple.home'))
           # D-48: 原子 view_count +1,SQL 层 `view_count = view_count + 1`,无 lost-update
           db.session.execute(
               update(Lemma).where(Lemma.id == fullcon.id).values(view_count=Lemma.view_count + 1)
           )
           db.session.commit()
           # D-39 携带 + D-69: 查 comments(时间倒序)和 backlinks(wiki 链接引用本词条)
           comments = Comment.query.filter_by(lemma_id=fullcon.id).order_by(Comment.time.desc()).all()
           related_lemmas = Lemma.query.filter(Lemma.content.contains('[[' + fullcon.title + ']]')).limit(10).all()
           return render_template(
               'detail.html',
               fullcon=fullcon,
               comments=comments,
               related_lemmas=related_lemmas,
           )
       ```

    关键点:
    - `methods=['GET']` 单 GET(D-65)
    - 入参 `request.args.get('title', '').strip()` 而非 `request.form.get('linklist')`(GET 化后 query 字符串)
    - 视图层传 `fullcon=fullcon` **单 Lemma 对象**(Plan 4.2 detail.html 用法)
    - 视图层不传 list,**不**用 `for fullcon in fullcontent` 循环(改完 detail.html 直接 `{{ fullcon.title }}`)
    - view_count +1 用 SQLAlchemy 2.x `update().where().values()`(D-63 决策,**不**用 `lemma.view_count += 1` 避免 race condition)
    - `db.session.commit()` 显式事务,view_count +1 与 detail 渲染同一事务
    - `related_lemmas` 用 `Lemma.content.contains('[[' + title + ']]')`(D-69,SQL `LIKE %[[Title]]%` 子串匹配,大小写敏感,D-69 决策)
    - `limit(10)`(D-70)
    - **不**传 `fullcontent` list(避免与新模板不一致)

    验证:`python -c "from app import create_app; a = create_app(); print([r.rule + ' ' + ','.join(r.methods - {'HEAD','OPTIONS'}) for r in a.url_map.iter_rules() if 'detail' in r.rule])"` 退出 0,输出 `/user/detail GET` 单条。
  </action>
  <acceptance_criteria>
    - `app/route/user.py` 顶部含 `from sqlalchemy import update`
    - `app/route/user.py:detail` 含 `@user.route('/detail', methods=['GET'])`(GET-only)
    - `app/route/user.py:detail` 含 `def detail():` 函数定义
    - `app/route/user.py:detail` 含 `request.args.get('title'` 或 `title = request.args.get(`
    - `app/route/user.py:detail` 含 `Lemma.query.filter_by(title=title).first()` 单对象查询
    - `app/route/user.py:detail` 含 `db.session.execute(update(Lemma).where(Lemma.id == fullcon.id).values(view_count=Lemma.view_count + 1))`(原子 +1)
    - `app/route/user.py:detail` 含 `db.session.commit()` 显式事务
    - `app/route/user.py:detail` 含 `Comment.query.filter_by(lemma_id=fullcon.id).order_by(Comment.time.desc()).all()`(评论倒序)
    - `app/route/user.py:detail` 含 `Lemma.query.filter(Lemma.content.contains('[[' + fullcon.title + ']]'))`(backlinks 查询)
    - `app/route/user.py:detail` 含 `.limit(10)`(backlinks 上限)
    - `app/route/user.py:detail` 含 `render_template('detail.html', fullcon=fullcon, comments=comments, related_lemmas=related_lemmas)`(三变量)
    - `app/route/user.py:detail` **不**含 `methods=['POST']`(POST 已删)
    - `app/route/user.py:detail` **不**含 `request.form.get('linklist')`(旧 POST 入参已删)
    - `app/route/user.py:detail` **不**含 `fullcontent = Lemma.query` 或 list 风格
    - `app/route/user.py:detail` **不**含 `lemma.view_count += 1` 风格(D-63 不用 Python 增量)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 注册 Jinja2 wikilink filter(D-50 / D-49 / D-51 / D-52)</name>
  <read_first>
    - `app/__init__.py`(L44 Flask app 工厂,确认 `app.jinja_env` 可用)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-49 红虚线,D-50 服务端不解析,D-51 正则,D-52 backlinks 单次查询)
    - 改写后的 `app/templates/detail.html`(Plan 4.2 Task 7,确认 `{{ fullcon.content | wikilink | safe }}` 引用)
  </read_first>
  <action>
    **改** `app/__init__.py`,在 `app = Flask(__name__)` 之后,L44 后(蓝图注册前),注册 Jinja2 filter:

    1. **加 import 块** (L1-8 已有):在 `import click` 后加:
       ```python
       import re
       from markupsafe import Markup, escape
       ```

    2. **加 filter 注册**(在 `app = Flask(__name__)` L44 后,蓝图注册 L65-67 前):
       ```python
       # D-50: Jinja2 wikilink filter 解析 [[xxx]] — 存在 → 蓝链接,不存在 → 红虚线 + "(创建此词条)"
       @app.template_filter('wikilink')
       def wikilink_filter(content):
           if not content:
               return content

           def render_wikilink(match):
               title = match.group(1).strip()
               if not title:
                   return match.group(0)
               # D-51: 标题转义防 XSS(markupsafe.escape 转 < > & 等)
               safe_title = escape(title)
               target = Lemma.query.filter_by(title=title).first()
               if target is not None:
                   return f'<a href="/user/detail?title={title}">{safe_title}</a>'
               else:
                   return (
                       f'<a class="wikilink-missing" '
                       f'style="text-decoration:underline dashed red;'
                       f'color:var(--pico-color-red-500);" '
                       f'href="/user/add?title={title}">'
                       f'{safe_title} (创建此词条)→</a>'
                   )

           # D-51: 非贪婪 + 不跨行
           return Markup(re.sub(r'\[\[([^\[\]\n]+?)\]\]', render_wikilink, content))
       ```

    3. **加 Lemma 导入**(L13 已有 `from app.api.model import db, User` 一行):追加 `Lemma`:
       ```python
       from app.api.model import db, User, Lemma
       ```

    关键点:
    - `app.template_filter('wikilink')` 是 Flask 标准 filter 注册方式
    - `re.sub(r'\[\[([^\[\]\n]+?)\]\]', render_wikilink, content)` — D-51 正则
    - `markupsafe.escape(title)` 防 XSS(标题里若含 `<` `>` `&` 不被解析)
    - `Lemma.query.filter_by(title=title).first()` 判存在(D-50)
    - 存在 → `<a href="/user/detail?title={title}">` 蓝链接(Pico.css 默认 `<a>` 颜色)
    - 不存在 → `<a class="wikilink-missing" style="..."> (创建此词条)→` 跳 `/user/add?title={title}`(D-49 决策)
    - `Markup(...)` 包装返回字符串,`{{ fullcon.content | wikilink | safe }}` 在 detail.html 不再二次 escape
    - **N+1 风险**(想法 D):filter 内部对每个不同 title 查一次 DB,词条有 N 个 wikilink 就有 N 次查询;Phase 4 demo 数据量小可接受。Plan 5 pytest smoke 不要求 N+1 防护(决策记录)

    验证:`python -c "from app import create_app; a = create_app(); print('wikilink' in a.jinja_env.filters)"` 退出 0 输出 `True`。
  </action>
  <acceptance_criteria>
    - `app/__init__.py` 含 `import re`
    - `app/__init__.py` 含 `from markupsafe import Markup, escape`
    - `app/__init__.py` 含 `from app.api.model import db, User, Lemma` (追加 Lemma)
    - `app/__init__.py` 含 `@app.template_filter('wikilink')` 装饰器
    - `app/__init__.py:wikilink_filter` 含 `def wikilink_filter(content):` 函数定义
    - `app/__init__.py:wikilink_filter` 含 `re.sub(r'\[\[([^\[\]\n]+?)\]\]'`(D-51 正则)
    - `app/__init__.py:wikilink_filter` 含 `Lemma.query.filter_by(title=title).first()`(存在性查询)
    - `app/__init__.py:wikilink_filter` 含 `/user/detail?title=` 存在时的蓝链接分支
    - `app/__init__.py:wikilink_filter` 含 `/user/add?title=` 不存在时的跳新建分支
    - `app/__init__.py:wikilink_filter` 含 `wikilink-missing` 类名 + `text-decoration:underline dashed red` 内联样式(D-49 红虚线)
    - `app/__init__.py:wikilink_filter` 含 `(创建此词条)` 文字
    - `app/__init__.py:wikilink_filter` 含 `Markup(...)` 包装
    - `app/__init__.py:wikilink_filter` 含 `markupsafe.escape` 或 `escape` 标题转义
    - `python -c "from app import create_app; a = create_app(); print('wikilink' in a.jinja_env.filters)"` 退出 0 输出 `True`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: /api/login 和 /api/logout 加 HX-Trigger 响应头(D-47)</name>
  <read_first>
    - `app/api/__init__.py`(L33-48 `/api/login` 和 `/api/logout` 当前实现)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-47 HX-Trigger 响应头)
    - .planning/phases/04-frontend-modernization-product-features/04-01-PLAN.md Task 4 创建的 `app/templates/base.html`(确认 `hx-trigger="nav-refresh from:body"` 监听已就绪)
  </read_first>
  <action>
    **改** `app/api/__init__.py`:

    1. **顶部 import 块**(L2):在 `from flask import ...` 行追加 `make_response`:
       ```python
       from flask import Blueprint, request, abort, redirect, url_for, flash, jsonify, current_app, make_response
       ```

    2. **改 `/api/login` 路由**(L33-42,登录成功分支):
       ```python
       @api.route('/login',methods=['POST'])
       def loginBusiness():
           name = request.form.get('name')
           password = request.form.get('password')
           nowUser = User.query.filter_by(name=name).first()
           if nowUser and check_password_hash(nowUser.password, password):
               login_user(nowUser)
               # D-47: 登录成功 → HX-Trigger nav-refresh 让 base.html nav 区域自动重渲染
               resp = make_response(redirect(url_for('apple.home')))
               resp.headers['HX-Trigger'] = 'nav-refresh'
               return resp
           flash('账号或密码错误')
           return redirect(url_for('apple.login'))
       ```

    3. **改 `/api/logout` 路由**(L44-48,登出成功分支):
       ```python
       @api.route('/logout', methods=['GET'])
       @login_required
       def logout():
           logout_user()
           resp = make_response(redirect(url_for('apple.home')))
           resp.headers['HX-Trigger'] = 'nav-refresh'
           return resp
       ```

    关键点:
    - `make_response(redirect(...))` 包装 redirect response,使其支持 `headers` 属性
    - `resp.headers['HX-Trigger'] = 'nav-refresh'` — HTMX 客户端识别此 header 后触发 `nav-refresh` 事件
    - base.html 已 `<div id="nav-right" hx-get="/api/nav-fragment" hx-trigger="nav-refresh from:body">`(Plan 4.1 Task 4),HTMX 自动监听并重新拉取
    - 失败分支(账号或密码错误)不设 HX-Trigger(无 nav 变化)
    - 失败仍走 flash + redirect(无 HTMX 端点要求)
  </action>
  <acceptance_criteria>
    - `app/api/__init__.py` import 块含 `make_response`
    - `app/api/__init__.py:loginBusiness` 含 `make_response(redirect(url_for('apple.home')))` 登录成功分支
    - `app/api/__init__.py:loginBusiness` 含 `resp.headers['HX-Trigger'] = 'nav-refresh'`
    - `app/api/__init__.py:logout` 含 `make_response(redirect(url_for('apple.home')))` 登出分支
    - `app/api/__init__.py:logout` 含 `resp.headers['HX-Trigger'] = 'nav-refresh'`
    - `app/api/__init__.py:loginBusiness` 失败分支不含 HX-Trigger(失败时 nav 不变)
    - `python -c "from app import create_app; a = create_app(); print('loginBusiness' in [r.endpoint for r in a.url_map.iter_rules()])"` 退出 0 输出 `True`(端点存在)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: 新增 /api/nav-fragment GET 端点(D-47)</name>
  <read_first>
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-47 nav-fragment 端点)
    - 改写后的 `app/templates/base.html`(Plan 4.1 Task 4,确认 `#nav-right` div 已就绪)
    - 现有 `app/templates/home.html` L29-37(nav 状态模式作为模板参考)
  </read_first>
  <action>
    **改** `app/api/__init__.py`,在 `/api/reset` 路由前(或文件末尾)新增 `/api/nav-fragment` GET 路由:

    1. **新建子模板** `app/templates/_nav_right.html`:
       ```html
       <ul>
         {% if current_user.is_authenticated %}
         <li><a href="#">欢迎您，{{ current_user.name }}</a></li>
         <li><a href="{{ url_for('apple.add') }}">写词条</a></li>
         <li><a href="{{ url_for('api.logout') }}">注销</a></li>
         {% else %}
         <li><a href="{{ url_for('apple.login') }}">登录</a></li>
         <li><a href="{{ url_for('apple.regist') }}">注册</a></li>
         {% endif %}
         <li><button id="theme-toggle" aria-label="切换主题">🌓</button></li>
       </ul>
       ```
       **注意**:base.html(Plan 4.1 Task 4)已写 `<button id="theme-toggle">` 在 header 右上角;`#nav-right` div 内的 `theme-toggle` 是 HTMX 重新拉取后保留切换按钮的兜底(防止 HTMX `hx-swap="outerHTML"` 替换 nav-right 时丢失 theme-toggle)。**executor 自决**:若 base.html 主题按钮在 nav-right **外**,本子模板不含 theme-toggle。

    2. **新增路由** (在 `app/api/__init__.py` 文件末尾,`reset` 路由前或后):
       ```python
       @api.route('/nav-fragment', methods=['GET'])
       def nav_fragment():
           # D-47: HTMX 监听 nav-refresh 时拉取;返 navbar 右侧片段
           return render_template('_nav_right.html')
       ```

    3. **顶部 import 块**(L2)追加 `render_template`:
       ```python
       from flask import Blueprint, request, abort, redirect, url_for, flash, jsonify, current_app, make_response, render_template
       ```

    关键点:
    - `/api/nav-fragment` 是 GET 端点(只读,无 CSRF 风险)
    - 返 HTML 片段(partial 模板),HTMX 客户端 `hx-swap="outerHTML"` 替换 `<div id="nav-right">` 内容
    - `current_user` 由 Flask-Login 全局注入(无需在视图层显式 `from flask_login import current_user`)
    - 子模板 `_nav_right.html` 配合 base.html 的 `<div id="nav-right" hx-get="/api/nav-fragment" hx-trigger="nav-refresh from:body">` 工作
  </action>
  <acceptance_criteria>
    - `app/templates/_nav_right.html` 存在
    - `app/templates/_nav_right.html` **不**含 `{% extends`(是 partial)
    - `app/templates/_nav_right.html` 含 `{% if current_user.is_authenticated %}` 和 `{% else %}`(登录态分支)
    - `app/templates/_nav_right.html` 含 `{{ current_user.name }}` 登录态显示
    - `app/templates/_nav_right.html` 含 `{{ url_for('apple.add') }}`(写词条)
    - `app/templates/_nav_right.html` 含 `{{ url_for('api.logout') }}`(登出)
    - `app/templates/_nav_right.html` 含 `{{ url_for('apple.login') }}`(登录)
    - `app/templates/_nav_right.html` 含 `{{ url_for('apple.regist') }}`(注册)
    - `app/api/__init__.py` 顶部 import 含 `render_template`
    - `app/api/__init__.py` 含 `@api.route('/nav-fragment', methods=['GET'])`
    - `app/api/__init__.py` 含 `def nav_fragment():` 函数定义
    - `app/api/__init__.py:nav_fragment` 含 `render_template('_nav_right.html')`
    - `python -c "from app import create_app; a = create_app(); print([r.rule for r in a.url_map.iter_rules() if 'nav' in r.rule])"` 退出 0 输出含 `/api/nav-fragment`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: /api/comment 检测 HX-Request 走 HTMX swap 返 _comment.html 片段(D-46 / D-73)</name>
  <read_first>
    - `app/api/__init__.py`(L82-104 `/api/comment` 当前实现)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-46, D-73)
    - .planning/phases/04-frontend-modernization-product-features/04-01-PLAN.md Task 7 创建的 `app/templates/_comment.html`
  </read_first>
  <action>
    **改** `app/api/__init__.py` `/api/comment` 路由(L82-104):

    1. **改函数体**:成功分支按 HX-Request 分流:
       ```python
       @api.route('/comment', methods=['POST'])
       @login_required
       def comment():
           lemma_id = request.form.get('lemma_id')
           content = (request.form.get('content') or '').strip()
           if not content or len(content) > 320:
               flash('评论内容不能为空且不超过 320 字符')
               return redirect(request.referrer or url_for('apple.home'))
           lemma = Lemma.query.get(lemma_id)
           if lemma is None:
               flash('词条不存在')
               return redirect(url_for('apple.home'))
           new_comment = Comment(
               user_id=current_user.id,
               lemma_id=lemma_id,
               content=content,
           )
           db.session.add(new_comment)
           db.session.commit()
           # D-73 step 3: 重新查询以 joined-load author backref
           new_comment = Comment.query.get(new_comment.id)
           # D-73: HX-Request → 返 _comment.html 片段(让 detail.html hx-swap afterbegin)
           if request.headers.get('HX-Request'):
               return render_template('_comment.html', comment=new_comment), 200
           # 向后兼容 Phase 3 整页刷新
           flash('评论发表成功')
           return redirect(request.referrer or url_for('apple.home'))
       ```

    关键点:
    - `if request.headers.get('HX-Request'):` 走 HTMX 路径,返 `_comment.html` 片段(200 OK)
    - 否则沿用 Phase 3 既定 flash + redirect
    - 失败(空内容 / 超长 / lemma 不存在)**始终**走 flash + redirect,**不**走 HX-Retarget(CD-14 决策,失败让用户用 Phase 2 统一错误页 / flash 感知)
    - D-46 决策:detail.html 评论 form 双轨(form `action="/api/comment"` 走 fallback + `hx-post="/api/comment"` 走 HTMX),HTMX 路径在响应返 200 + HTML 时 `hx-swap="afterbegin"` 插入
    - **不**改 `db.session.add(new_comment)` / `db.session.commit()` 顺序
    - **不**改 `Comment(user_id=..., lemma_id=..., content=...)` 参数
    - **不**改 `@login_required` 装饰器(D-26 携带,匿名 POST 仍 401/403 走统一错误页)

    验证:`grep -F 'HX-Request' app/api/__init__.py` 至少 1 行(comment 路由分支)。
  </action>
  <acceptance_criteria>
    - `app/api/__init__.py:comment` 含 `if request.headers.get('HX-Request'):` 分支
    - `app/api/__init__.py:comment` 代码中 `render_template('_comment.html', ...)` 之前**必须**出现 `new_comment = Comment.query.get(new_comment.id)` 重查语句;否则 author.name 渲染 AttributeError
    - `app/api/__init__.py:comment` 含 `render_template('_comment.html', comment=new_comment)` HX 分支
    - `app/api/__init__.py:comment` HX 分支含 HTTP 状态码 200(`), 200` 在 tuple 第二位)
    - `app/api/__init__.py:comment` 含 `flash('评论发表成功')` 走非 HX 路径(Phase 3 兼容)
    - `app/api/__init__.py:comment` 含 `redirect(request.referrer or url_for('apple.home'))` 非 HX 路径
    - `app/api/__init__.py:comment` **不**改 `@login_required` 装饰器
    - `app/api/__init__.py:comment` **不**改匿名/超长/lemma 不存在分支(失败仍走 flash + redirect)
    - `python -c "from app import create_app; a = create_app(); print([r.rule + ' ' + ','.join(r.methods - {'HEAD','OPTIONS'}) for r in a.url_map.iter_rules() if r.rule == '/api/comment'])"` 退出 0 输出 `/api/comment POST`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: 验证 /user/add 视图传 prefill_title(D-49 wikilink 红色虚线跳新建)</name>
  <read_first>
    - `app/route/user.py`(L24-27 `/user/add` 当前实现)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-49 wikilink 红色虚线跳 /user/add?title=)
    - 改写后的 `app/templates/add.html`(Plan 4.1 Task 5,确认 `name="title" value="{{ prefill_title or '' }}"` 已就绪)
  </read_first>
  <action>
    **改** `app/route/user.py` `/user/add` 路由(L24-27)传 `prefill_title` 给模板:

    ```python
    @user.route('/add')
    @login_required
    def add():
        # D-49: 接受 ?title=... query 参数预填到 title input
        prefill_title = request.args.get('title', '').strip()
        return render_template('add.html', prefill_title=prefill_title)
    ```

    关键点:
    - `prefill_title = request.args.get('title', '').strip()` — wikilink 红色虚线跳 `/user/add?title=foo`,视图层读出传给模板
    - add.html(Plan 4.1 Task 5)已含 `<input type="text" name="title" value="{{ prefill_title or '' }}" required maxlength="40">`,自动接收
    - 关键 URL 形如 `http://127.0.0.1:5000/user/add?title=新词条` → 打开 add.html 时 title 字段已预填"新词条"

    **不**改其他细节:
    - 仍然 `@login_required` 装饰器
    - 仍然只支持 GET method(`methods` 默认 GET)
  </action>
  <acceptance_criteria>
    - `app/route/user.py:add` 含 `prefill_title = request.args.get('title', '').strip()` 预填读取
    - `app/route/user.py:add` 含 `render_template('add.html', prefill_title=prefill_title)` 传模板
    - `app/route/user.py:add` 仍含 `@login_required` 装饰器
    - `app/templates/add.html` 含 `value="{{ prefill_title or '' }}"` (Plan 4.1 Task 5 已就绪,验证)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 8: 验证 init_db() 自动覆盖新 schema(D-64)</name>
  <read_first>
    - `app/api/model.py:init_db`(L70-85 既有实现)
    - .planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md (D-64 不引入 alembic,走 drop+create)
  </read_first>
  <action>
    **验证**(不修改):确认 `init_db()` 函数体覆盖新增的 `Lemma.updated_at` + `Lemma.view_count` 两列:

    1. **不**修改 `init_db()` 函数体(D-64 决策)
    2. **验证逻辑**:`db.drop_all()` + `db.create_all()` 走 SQLAlchemy 2.x ORM 元数据,自动读取 `Lemma` 当前所有 `db.Column` 声明(含新加的 `updated_at` / `view_count`),MySQL `CREATE TABLE` 语句自动生成新列
    3. **灌种子兼容性**:7 条种子 `Lemma` 走 `Lemma(title='xxx', content='xxx')` 构造,**不**传 `view_count` / `updated_at` — 走 column default + server_default 双兜底,落库 `view_count=0` `updated_at=<utcnow>`
    4. **不**追加 `db.session.add(Lemma(title='a', content='b', view_count=0))` 之类显式赋值(CLAUDE.md 原则 3:不"改善"无关代码)

    验证命令:
    - `python -c "from app import create_app; a = create_app(); ctx = a.app_context(); ctx.push(); from app.api.model import db, init_db; init_db(); print('ok')"` 退出 0(若本地有 MySQL)。否则静态验证 `init_db` 函数体不需改。
  </action>
  <acceptance_criteria>
    - `app/api/model.py:init_db` 函数体未改
    - `app/api/model.py:init_db` 仍含 `db.drop_all()` 和 `db.create_all()`
    - `app/api/model.py:init_db` 仍灌 7 条种子 Lemma
    - 种子 `Lemma(title='...', content='...')` 构造不显式传 `view_count` 或 `updated_at`(D-67 走 column default)
    - 没有 `import alembic` / `import flask_migrate` 之类迁移库
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 9: 验证 detail.html / base.html 模板与新视图层 + filter 一致性</name>
  <read_first>
    - 改写后的 `app/templates/detail.html`(Plan 4.2 Task 7)
    - 改写后的 `app/templates/base.html`(Plan 4.1 Task 4)
    - 改写后的 `app/templates/_comment.html`(Plan 4.1 Task 7)
    - 改写后的 `app/templates/_nav_right.html`(本 plan Task 5)
    - 改写后的 `app/api/model.py`(本 plan Task 1)
    - 改写后的 `app/route/user.py`(本 plan Task 2 / Task 7)
  </read_first>
  <action>
    **验证**(不修改,只读):确认所有改动后,模板 + 视图层 + 模型一致,无 missing variable / undefined filter:

    1. **detail.html 引用变量 / filter** 全部就绪:
       - `{{ fullcon.title }}` — Task 2 视图层传 `fullcon` 单对象
       - `{{ fullcon.updated_at.strftime('%Y-%m-%d %H:%M') if fullcon.updated_at else '—' }}` — Task 1 列已加
       - `{{ fullcon.view_count }}` — Task 1 列已加
       - `{{ fullcon.content | wikilink | safe }}` — Task 3 filter 已注册
       - `{% for comment in comments %}{% include '_comment.html' %}` — 视图层 Task 2 传 `comments`,Plan 4.1 _comment.html 已就绪
       - `{% if related_lemmas %}{% for related in related_lemmas %}` — 视图层 Task 2 传 `related_lemmas`
       - `{{ url_for('api.modify') }}` / `{{ url_for('admin.delete_lemma', lemma_id=fullcon.id) }}` — Phase 2 既有端点未变
       - `{{ url_for('api.comment') }}` + `hx-post="{{ url_for('api.comment') }}"` — Phase 3 端点 Task 6 改 HX 分支

    2. **base.html 引用**:
       - `id="nav-right"` + `hx-get="/api/nav-fragment"` + `hx-trigger="nav-refresh from:body"` — Plan 4.1 Task 4 已就绪,Task 5 端点已实现
       - `id="theme-toggle"` — Plan 4.1 Task 4 已就绪(theme 切换 JS)
       - `url_for('apple.home')` / `url_for('apple.add')` — 既有端点未变

    3. **result.html 引用**(Plan 4.2 Task 4 改写,本 plan 不动):
       - `{{ result.view_count }}` / `{{ result.updated_at }}` — Task 1 列已加
       - `{{ url_for('apple.detail', title=result.title) }}` — Task 2 detail 改 GET 后 url_for 正常

    4. **_search_result.html 引用**(Plan 4.2 Task 5 创建,本 plan 不动):
       - `{{ lemma.view_count }}` / `{{ lemma.updated_at }}` — Task 1 列已加
       - `{{ url_for('apple.detail', title=lemma.title) }}` — Task 2 改 GET 后正常

    5. **/api/comment 端点**(Task 6):HTTX 路径返 `_comment.html` 片段,字段访问 `{{ comment.author.name }}` / `{{ comment.content }}` / `{{ comment.time.strftime(...) }}` 与 `_comment.html` 模板(Plan 4.1 Task 7)一致

    验证命令(若本地有 Flask + MySQL):
    - `curl -s 'http://127.0.0.1:5000/user/detail?title=123' -H 'Cookie: ...' -L` 返 200 + 含 wikilink filter 输出 + 含 "最后编辑于" + 含 "浏览 X 次" + 含 `<aside>相关词条</aside>`
    - 模拟 HTMX 评论:`curl -s -X POST -H 'HX-Request: true' -H 'Content-Type: application/x-www-form-urlencoded' -d 'lemma_id=1&content=test&csrf_token=...' http://127.0.0.1:5000/api/comment` 返 200 + 含 `<article` (评论卡片片段)
    - 模拟登录触发 nav-refresh:`curl -i -X POST -d 'name=a&password=a&csrf_token=...' http://127.0.0.1:5000/api/login` 响应头含 `HX-Trigger: nav-refresh`
  </action>
  <acceptance_criteria>
    - `grep -F "{{ fullcon.updated_at" app/templates/detail.html` 退出 0
    - `grep -F "{{ fullcon.view_count }}" app/templates/detail.html` 退出 0
    - `grep -F "| wikilink | safe" app/templates/detail.html` 退出 0
    - `grep -F "{% if related_lemmas %}" app/templates/detail.html` 退出 0
    - `grep -F "{% include '_comment.html' %}" app/templates/detail.html` 退出 0
    - `grep -F "{{ url_for('apple.detail', title=" app/templates/result.html` 退出 0
    - `grep -F "hx-get=\"/api/nav-fragment\"" app/templates/base.html` 退出 0
    - `grep -F "hx-trigger=\"nav-refresh from:body\"" app/templates/base.html` 退出 0
    - `grep -F "HX-Request" app/api/__init__.py` 至少 1 行(comment 路由)
    - `grep -F "HX-Trigger" app/api/__init__.py` 至少 2 行(login + logout)
    - `grep -F "update(Lemma)" app/route/user.py` 至少 1 行(view_count 原子 +1)
    - `grep -F "view_count=Lemma.view_count + 1" app/route/user.py` 退出 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
完成 Plan 4.3 后,做以下端到端验证(覆盖 LEMMA-01..08 + FRONT-06):

1. **Schema 变更** (LEMMA-01 / LEMMA-06 基础):
   - 启动前访问 `GET /api/reset` (dev mode) 触发 `init_db()`
   - `python -c "from app.api.model import Lemma, db; from app import create_app; a=create_app(); ctx=a.app_context(); ctx.push(); print([c.name for c in Lemma.__table__.columns])"` 退出 0 输出含 `'updated_at'` `'view_count'`
   - MySQL 直接查: `DESCRIBE lemma` 输出含 `updated_at datetime NOT NULL` + `view_count int NOT NULL DEFAULT '0'`

2. **view_count 原子 +1** (LEMMA-05):
   - 访问 `/user/detail?title=123` 一次(注意 LEMMA-05 +1 也在同一请求里,渲染后 view_count 已是 1)
   - 访问 `/user/detail?title=123` 第二次,DB `SELECT view_count FROM lemma WHERE title='123'` 返回 2
   - 20 并发 `curl -s 'http://127.0.0.1:5000/user/detail?title=123' &` × 20 → DB view_count = 22(原子 +1,无 lost-update)
   - `grep -F 'view_count=Lemma.view_count + 1' app/route/user.py` 退出 0(SQL 原子增量)

3. **updated_at 自动维护** (LEMMA-06):
   - 创建 lemma:`POST /api/add` with `title=新词条&content=<p>x</p>`(登录用户)→ DB 查 `SELECT updated_at FROM lemma WHERE title='新词条'` 返回非空 timestamp
   - 1 分钟后 `POST /api/modify` with `newTitle=新词条&newContent=<p>xx</p>` → DB updated_at 自动更新为新 timestamp(SQLAlchemy `onupdate=utcnow` 触发)
   - `python -c "from app.api.model import Lemma, db; from app import create_app; a=create_app(); ctx=a.app_context(); ctx.push(); l=Lemma(title='x', content='y'); db.session.add(l); db.session.commit(); print(l.updated_at, l.view_count)"` 退出 0,输出 `updated_at <timestamp> view_count 0`

4. **wikilink filter** (LEMMA-07 / FRONT-06):
   - 创建 lemma 含 `[[123]]`: `POST /api/add` with `title=测试&content=<p>参考资料 [[123]]</p>`(bleach 不删 `[[` `]]` 因为不是 HTML 标签)
   - 访问 `/user/detail?title=123` → HTML 含 `<a href="/user/detail?title=123">123</a>` 蓝链接(123 词条存在)
   - 访问 `/user/detail?title=测试` → HTML 含 `wikilink-missing` 类(123 词条已存在,所以不显示红虚线,若改访问不存在的目标 `[[不存在]]` 应显示红虚线)
   - 创建 lemma 含 `[[不存在的目标]]`:`POST /api/add` with `title=红虚线测试&content=<p>[[不存在的目标]]</p>`(lemma_id=8)
   - 访问 `/user/detail?title=红虚线测试` → HTML 含 `wikilink-missing` + `text-decoration:underline dashed red` + `(创建此词条)` + `<a href="/user/add?title=不存在的目标">`(红虚线 + 跳新建)
   - 验证:点击红虚线跳 `/user/add?title=不存在的目标` → 页面 title input 自动填 "不存在的目标"(`prefill_title` 工作)
   - 验证 XSS 防护: 创建 lemma 含 `[[<script>alert(1)</script>]]`,`POST /api/add` with `title=xss测试&content=<p>[[<script>alert(1)</script>]]</p>` → DB 存原样(wikilink filter 不会把 `<script>` 标签当 wikilink target,只会按 `[[...]]` 模式匹配内部文字);访问 detail → 浏览器**不**弹 alert,wikilink filter 内 `markupsafe.escape(title)` 把 `<` 转 `&lt;` 等
   - `grep -F "markupsafe.escape" app/__init__.py` 退出 0(转义防 XSS)
   - `grep -F "re.sub(r'\\[\\[" app/__init__.py` 退出 0(D-51 正则)

5. **backlinks 相关词条** (LEMMA-08):
   - 创建 lemma A `[[123]]`(已存在 lemma "123"),然后 lemma B 也 `[[123]]`,lemma C 也 `[[123]]`
   - 访问 `/user/detail?title=123` → 页面底部 `<aside>相关词条</aside>` 含 A / B / C 三个 link(`related_lemmas` 列表)
   - 限制:`related_lemmas | length <= 10`(D-70 决策)
   - 若 lemma D 不引用 123 但引用 456,访问 detail?title=123 → related_lemmas **不**含 D
   - 词条 D 内容里 `[[123]]` 大小写不一致(如 `[[123]]` 写在 lemma "123abc" 内)→ 大小写敏感(D-69 决策),不匹配
   - `grep -F "Lemma.content.contains('[[' + fullcon.title + ']]')" app/route/user.py` 退出 0

6. **HTMX 评论 swap** (D-46):
   - 登录 a/a → 访问 detail → 在评论 textarea 输入 "HTMX测试"
   - 提交评论(form 自动走 HTMX `hx-post` 路径)
   - 浏览器 DevTools Network 看到 `POST /api/comment` 请求头 `HX-Request: true` + 响应 200 + HTML 是 `<article id="comment-N"...` 片段
   - 新评论卡片自动 `hx-swap="afterbegin"` 插入到 `#comments-list` 顶部
   - textarea 自动清空(`hx-on::after-request="if(event.detail.successful) this.reset()"`)
   - 浏览器未启用 JS 走整页 fallback(form `action="/api/comment"` 走浏览器原生 POST,Phase 3 兼容行为)
   - `grep -F "render_template('_comment.html', comment=new_comment)" app/api/__init__.py` 退出 0

7. **登录态 HTMX 刷新** (D-47):
   - 浏览器未登录访问首页 → 导航栏右侧看到"登录" "注册" 链接
   - 在另一标签页 `curl -i -X POST -d 'name=a&password=a&csrf_token=...' http://127.0.0.1:5000/api/login`(或直接浏览器登录)→ 响应头含 `HX-Trigger: nav-refresh`(HTTP/1.1 302 Found 响应)
   - 浏览器原标签页(`base.html` HTMX 监听 `nav-refresh from:body`)**自动** 拉取 `/api/nav-fragment` GET 替换 `#nav-right` 内容为 "欢迎您,a" + "写词条" + "注销"
   - 整页不刷新,只 nav 区域局部更新
   - `curl -i -X GET http://127.0.0.1:5000/api/nav-fragment` 返 200 + 含 `<ul>` 片段(HTMX 解析用)
   - 登出后(GET `/api/logout`)响应头也含 `HX-Trigger: nav-refresh`,nav 区域自动变回"登录" "注册"
   - `grep -F "HX-Trigger" app/api/__init__.py` 至少 2 行(login + logout)
   - `grep -F "def nav_fragment" app/api/__init__.py` 退出 0

8. **detail 路由 GET 化** (D-65):
   - `curl -s 'http://127.0.0.1:5000/user/detail?title=123'` 返 200 + 含 lemma 内容 + 含 wikilink filter 输出
   - `curl -s -X POST http://127.0.0.1:5000/user/detail -d 'linklist=123'` 返 405 Method Not Allowed(GET-only 路由拒绝 POST)
   - result.html 中点击 lemma 链接 → 浏览器地址栏 URL 形如 `/user/detail?title=123`(query 字符串,非 POST 隐藏 form)
   - `grep -F "methods=['GET']" app/route/user.py:detail` 退出 0

9. **app 启动 + 全 URL 注册** (Phase 5 pytest smoke 准备):
   - `python -c "from app import create_app; a = create_app(); print(sorted([r.rule + ' ' + ','.join(r.methods - {'HEAD','OPTIONS'}) for r in a.url_map.iter_rules() if 'detail' in r.rule or 'nav-fragment' in r.rule or 'search' in r.rule or 'add' in r.rule or 'modify' in r.rule]))"` 退出 0,输出含:
     - `/user/detail GET`
     - `/user/search GET`
     - `/user/add GET`
     - `/user/modify GET`
     - `/api/add POST`
     - `/api/modify POST`
     - `/api/login POST`
     - `/api/logout GET`
     - `/api/comment POST`
     - `/api/comment/<int:comment_id>/delete POST`
     - `/api/nav-fragment GET`
     - `/api/admin/comment/<int:comment_id>/delete POST`
     - `/api/admin/lemma/<int:lemma_id>/delete POST`
</verification>

<artifacts_this_phase_produces>
- `app/api/model.py:Lemma.updated_at` (new column, db.DateTime, default+onupdate=utcnow, nullable=False)
- `app/api/model.py:Lemma.view_count` (new column, db.Integer, default=0, server_default='0', nullable=False)
- `app/api/model.py:init_db()` (no logic change; auto-covers new columns per D-64)
- `app/route/user.py:detail` (modify — POST → GET `?title=...`, atomic view_count +1 via `update(Lemma)`, pass `fullcon` single object + `comments` + `related_lemmas` to template)
- `app/route/user.py:add` (modify — read `?title=` query param, pass `prefill_title` to template)
- `app/route/user.py:search` (no change; Plan 4.2 already GET-ified)
- `app/route/user.py` import (add `from sqlalchemy import update`)
- `app/api/__init__.py:loginBusiness` (modify — wrap redirect in `make_response`, set `HX-Trigger: nav-refresh` header on success)
- `app/api/__init__.py:logout` (modify — wrap redirect in `make_response`, set `HX-Trigger: nav-refresh` header)
- `app/api/__init__.py:comment` (modify — detect HX-Request header, render `_comment.html` partial on success, fall back to flash+redirect)
- `app/api/__init__.py:nav_fragment` (new — GET endpoint returning `_nav_right.html` partial for HTMX nav-refresh)
- `app/api/__init__.py` import (add `make_response`, `render_template`)
- `app/__init__.py` (modify — import `re`, `markupsafe.Markup`, `markupsafe.escape`, `Lemma`; register `@app.template_filter('wikilink')` that regex-parses `[[...]]` and renders existing-as-blue-link / missing-as-red-dashed-with-create-affordance)
- `app/templates/_nav_right.html` (new — partial, nav-right fragment with login state branches)
- `app/templates/detail.html` (no change from Plan 4.2 — already references `fullcon.updated_at`, `fullcon.view_count`, `|wikilink|safe`, `related_lemmas`, includes `_comment.html`)
- `app/templates/base.html` (no change from Plan 4.1 — already has `hx-get="/api/nav-fragment"` + `hx-trigger="nav-refresh from:body"` listener)
- `app/templates/_comment.html` (no change from Plan 4.1 — already exists for HTMX swap)
- Schema columns added: `lemma.updated_at` (DateTime, NOT NULL, default+onupdate=utcnow), `lemma.view_count` (Integer, NOT NULL, server_default='0', default=0)
- URL rules registered (new): `/api/nav-fragment GET`
- URL rules registered (modified): `/user/detail` (POST → GET)
- URL rules registered (unchanged): `/user/search GET` (Plan 4.2), `/user/add GET`, `/user/modify GET`, `/api/add POST`, `/api/modify POST`, `/api/login POST`, `/api/logout GET`, `/api/comment POST`, `/api/comment/<int:comment_id>/delete POST`, `/api/admin/comment/<int:comment_id>/delete POST`, `/api/admin/lemma/<int:lemma_id>/delete POST`
- New Jinja2 filter: `wikilink` (registered via `@app.template_filter` on `app/__init__.py`)
- New template: `app/templates/_nav_right.html` (partial for `/api/nav-fragment` HTMX swap)
</artifacts_this_phase_produces>

<threat_model>
ASVS L1 review for Plan 4.3:

1. **T1: Wikilink content XSS (URL-encoding / HTML injection in `[[...]]` target)**
   - Vector: 攻击者创建 lemma 含 `[[<script>alert(1)</script>]]`,`Lemma.content` 存原文,detail.html `{{ fullcon.content | wikilink | safe }}` 调用 filter,filter 内 `Lemma.query.filter_by(title='<script>alert(1)</script>').first()` 查无,返回红虚线 `<a href="/user/add?title=<script>alert(1)</script>">`,URL 注入 + HTML 注入双重。
   - Mitigation: **filter 内 `markupsafe.escape(title)`**(Task 3):对 match.group(1) 字符串 HTML-escape `<` `>` `&` `"` 等。模板最终输出 `<a href="/user/add?title=&lt;script&gt;alert(1)&lt;/script&gt;">` — URL 中 `<` 是合法的 query 字符,但浏览器不会解析为 HTML 标签。同时 `{{ url_for('apple.add', title=...) }}` 的 Flask url_for 自动 percent-encode(若使用 url_for,本 plan 模板用法是字符串拼接 `/user/add?title={title}`,不走 url_for,但 escape 仍兜底 XSS)。
   - **残余风险**:`/user/add?title=<script>...` URL 中字符被 escape 后是 `&lt;` 等,浏览器地址栏接收的是字面 `&lt;` 不是 `<`,OK。
   - Test: 创建 `[[<img src=x onerror=alert(1)>]]` lemma → 访问 detail → 浏览器**不**执行 onerror,wikilink-missing 链接显示转义后的文字。

2. **T2: view_count race condition (concurrent GETs)**
   - Vector: 20 个用户同时访问 `/user/detail?title=123`,Python `lemma.view_count += 1; db.session.commit()` 模式(读-改-写两步)在并发下丢失更新,view_count 累加不正确。
   - Mitigation: **D-48 SQLAlchemy 2.x `update(Lemma).where(Lemma.id == lemma.id).values(view_count=Lemma.view_count + 1)`**(Task 2):DB 层原子自增,MySQL `UPDATE lemma SET view_count = view_count + 1 WHERE id = ?` 是单条原子 SQL,**不**经过 Python 层。配合 `db.session.commit()` 显式事务,失败回滚不影响 detail 渲染。
   - Test: 20 并发 `curl -s 'http://127.0.0.1:5000/user/detail?title=123' &` × 20 → DB `SELECT view_count FROM lemma WHERE title='123'` 恰为 21(初始 0 + 1 init_db + 20 并发)。**不**走 Python `lemma.view_count += 1`。

3. **T3: Backlinks regex injection (pattern `[[...]]` matches malicious content)**
   - Vector: Lemma.content 来自 DB,已是 bleach 过滤的 HTML,但可能含 `[[\n]]` 或 `[[a]b]c]]` 等边界 case。
   - Mitigation: **D-51 正则 `r'\[\[([^\[\]\n]+?)\]\]'`**(Task 3):`[^\[\]\n]` 排除 `[` `]` `\n` 字符,匹配内容不含括号和换行,`+?` 非贪婪。攻击者输入 `[[a]] [[b]]` 不会匹配成整段。`title.strip()` 在 `match.group(1)` 后去首尾空白。
   - **残余风险**:`[[a]b]c]]` 这种畸形输入匹配失败,filter 跳过(原文 `[[a]b]c]]` 保留作为普通文本),不构成 XSS 也不功能破坏。
   - Test: 创建 lemma content `[[a]] [[b]]\n[[c]]` → 访问 detail → 3 个独立 wikilink,a/b/c 各自判断存在性。

4. **T4: nav-fragment CSRF (state-mutating GET endpoint)**
   - Vector: `/api/nav-fragment` 是 GET 端点,有人构造跨站 `<img src="/api/nav-fragment">` 让登录用户请求该 URL。
   - Mitigation: GET 端点**不**改状态,仅渲染 nav 片段,即使被 CSRF 调用也**不**造成数据修改,只会泄漏当前用户登录态(显示"欢迎您,a"等公开信息,无敏感)。**不**需 CSRF token(GET 不在 Flask-WTF CSRFProtect 拦截范围)。
   - Test: `curl -i -X POST http://127.0.0.1:5000/api/nav-fragment` 返 405 Method Not Allowed(GET-only)。

5. **T5: HX-Trigger header injection (login/logout 改 header)**
   - Vector: 攻击者让受害者在跨站上下文调用 `/api/login`,响应 `HX-Trigger: nav-refresh` 头被受害者的 HTMX 拦截,触发 nav 区域重渲染显示错误登录态。
   - Mitigation: HX-Trigger 是普通 HTTP header,**只**对**已加载 HTMX** 的页面有意义,且只触发**当前页面监听**的 `nav-refresh` 事件(不跨页面传播)。**不**构成 XSS / 数据篡改。CSRF token 在表单上(D-09 携带,login form 已有),`/api/login` POST 无 token 返 400 走统一错误页。
   - Test: 不带 csrf_token `curl -X POST -d 'name=a&password=a' http://127.0.0.1:5000/api/login` 返 400(Phase 2 CSRF 兜底),不会触发 nav-refresh。

6. **T6: /api/comment 失败路径走整页还是 HTMX(分支逻辑)**
   - Vector: 攻击者构造 HTMX POST 评论但 lemma_id 不存在,期望 `/api/comment` 返 200 + 错误片段,让用户以为成功。
   - Mitigation: **D-73 失败始终走 flash + redirect**(Task 6):`lemma is None` 走 `flash('词条不存在')` + `redirect(url_for('apple.home'))`,**不**走 HX 分支。客户端 HTMX 看到 302 redirect,按 hx-target/hx-swap 跳到 home,不显示伪成功。**不**返 `HX-Retarget: #comment-form` 重写 form(CD-14 决策,简化 UX)。

7. **T7: init_db() 重灌数据丢失(用户编辑的内容没了)**
   - Vector: 部署后访问 `/api/reset` 触发 `init_db()` drop_all + create_all + 灌种子,用户编辑的 lemma 全部丢失。
   - Mitigation: **D-13/D-14 携带**:`/api/reset` 路由在 `app.debug=False` 时 `abort(404)`(Phase 2 D-14)。生产部署走 `flask init-db` CLI 而非 `/api/reset`。Phase 5 Dockerfile 入口 `flask init-db`(Phase 5 D-08),**不**暴露 `/api/reset` 路径。Phase 4 demo 接受重灌(决策记录在 D-64 + PROJECT.md Key Decisions "测试仅 smoke")。
   - Test: `curl -i http://127.0.0.1:5000/api/reset`(gunicorn 启动,非 debug 模式) 返 404。

8. **T8: wikilink filter 内部 DB 查询 N+1(性能 / 拒绝服务)**
   - Vector: Lemma.content 有 100 个 `[[xxx]]` 引用,filter 内部对每个不同 title 调 `Lemma.query.filter_by(title=...)` 100 次,DB 慢查询。
   - Mitigation: **D-52 单次查询思路 + 决策记录 N+1 可接受**:Phase 4 demo 数据量小(< 10 个 lemma),N+1 不致命。`想法 D` 显式记录"Phase 4 demo 数据量小可接受,生产环境再考虑批量查询优化"。**不**做缓存(CD-14 决策,缓存失效逻辑不值)。
   - Test: 静态 grep `Lemma.query.filter_by` 在 wikilink filter 函数体内仅 1 处(每次 match 调一次,D-52 决策)。

Risk: T1 + T2 是高危(每个 detail GET 都触发);T3 / T6 是逻辑分支;其余是 UX 兜底。
</threat_model>
