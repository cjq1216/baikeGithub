---
context: phase
phase: 05-docker-deployment-tests-acceptance
task: discuss-phase complete
total_tasks: 2 plans (planning not yet started)
status: ready_to_plan
last_updated: 2026-06-12T08:28:35.003Z
---

<current_state>
Phase 5 (Docker Deployment, Tests & Acceptance) discuss-phase **已完成**。CONTEXT.md + DISCUSSION-LOG.md 已落盘但**未 git commit**。STATE.md 未更新。session 因上下文耗尽(80%+)在 commit 步骤前暂停。
</current_state>

<completed_work>

讨论环节(9/9):
- ✓ Phase 5 上下文初始化 (gsd-tools init.phase-op)
- ✓ 反模式检查 (.continue-here.md 不存在)
- ✓ SPEC.md / CONTEXT.md / CHECKPOINT 检查 (均无)
- ✓ 加载先前上下文:PROJECT.md / REQUIREMENTS.md / STATE.md / ROADMAP.md / Phase 2 + 3 + 4 CONTEXT.md (D-01..D-74 全部生效)
- ✓ Todo 交叉引用 (无 Phase 5 匹配)
- ✓ 代码库扫描 (.planning/codebase/STACK.md, STRUCTURE.md, TESTING.md)
- ✓ 灰色地带分析(4 候选)+ 用户选择 2(Docker entrypoint + README)
- ✓ 区域 1 深入 3 问 → D-75/76/77
- ✓ 区域 2 深入 3 问 → D-78/79/80
- ✓ 未选区域走 Claude 默认 → CD-21..CD-40
- ✓ 写 .planning/phases/05-docker-deployment-tests-acceptance/05-CONTEXT.md (D-75..D-88 + CD-21..CD-40)
- ✓ 写 .planning/phases/05-docker-deployment-tests-acceptance/05-DISCUSSION-LOG.md
</completed_work>

<remaining_work>

收尾环节(2 项):
- ⏸ Git commit: `docs(05): capture phase context` 含 05-CONTEXT.md + 05-DISCUSSION-LOG.md + HANDOFF.json + .continue-here.md
- ⏸ STATE.md 更新:`gsd-tools query state.record-session --stopped-at "Phase 5 context gathered" --resume-file ".planning/phases/05-docker-deployment-tests-acceptance/05-CONTEXT.md"`

下一阶段(新 session):
- ⏸ `/gsd-plan-phase 5` 读取 05-CONTEXT.md,产出 05-01-PLAN.md (Docker 化) + 05-02-PLAN.md (pytest + README)
</remaining_work>

<decisions_made>

**用户讨论锁定(D-75..D-80):**
- D-75: Docker entrypoint init-db 走"检表行数 + `--if-empty`"幂等模式 — Phase 2 D-15 的 `init_db()` 函数签名加 `if_empty=False`,CLI 加 `--if-empty` flag,容器重启不清生产数据
- D-76: MySQL 启动等待 — entrypoint 内联 Python stdlib socket 重试 30s,超时 exit 1,无外部依赖
- D-77: entrypoint 走 sh 脚本 + root 用户(最简,demo 性质 + 外部 nginx 隔离)
- D-78: README 面向资深运维(~150 行,假定 Docker/MySQL/nginx 基线知识)
- D-79: TEST-05 验收清单全进 README.md(单入口,不拆 DEPLOY.md/ACCEPTANCE.md)
- D-80: docker-compose.example.yml 仅含 app service(不含 MySQL),nginx 只给 server snippet inline

**Phase 5 ROADMAP locked + Claude 默认锁定(D-81..D-88 + CD-21..CD-40):**
- D-81/82/83: Dockerfile 多阶段(builder + runtime),python:3.11-slim,COPY --from=builder,WORKDIR /app,CMD ["./entrypoint.sh"]
- D-84: .dockerignore 排除 .planning/.git/tests/.venv/instance/.flask_secret 等,< 50MB
- D-85: test_smoke.py 9 步覆盖(regist→login→add→search→detail→modify→comment→delete→logout)
- D-86: test_admin.py 覆盖 admin 删 lemma/comment + 非 admin 403
- D-87: requirements.txt 新增 gunicorn + pytest + pytest-flask
- D-88: ProxyFix 通过 `FLASK_BEHIND_PROXY=true` env 条件启用
- CD-21..CD-24: gunicorn workers=2 threads=4 timeout=30 + ProxyFix(x_for=1) + 不增 /healthz + 日志 stdout
- CD-25..CD-29: pytest SQLite in-memory + module-level app + WTF_CSRF_ENABLED=False + test_admin/smoke 分离 + HTMX 路径不覆盖
- CD-30..CD-40: Makefile 简洁(test/smoke/docker-build/docker-run)+ conftest 在 tests/ + 不装调试工具 + .env 占位符等

详细见 `.planning/phases/05-docker-deployment-tests-acceptance/05-CONTEXT.md`
</decisions_made>

<blockers>
无。所有决策清晰,可直接进入 `/gsd-plan-phase 5`。
</blockers>

## Required Reading (in order)

1. `.planning/phases/05-docker-deployment-tests-acceptance/05-CONTEXT.md` — Phase 5 所有实施决策 + canonical refs + 已锁定 Phase 1-4 决策清单
2. `.planning/PROJECT.md` § Core Value — 第三方 README + docker run + 演示端到端 = Phase 5 验收标准
3. `.planning/REQUIREMENTS.md` § Tests & Acceptance + Infrastructure — TEST-01..05 + INFRA-07/08/10/11 需求文本
4. `.planning/ROADMAP.md` § Phase 5 — 8 条 SUCCESS 标准 + 计划 5.1/5.2 拆分
5. `.planning/phases/02-security-auth-hardening/02-CONTEXT.md` § D-13..D-25 — `init_db()` 函数 + CLI 既有形态,Phase 5 D-75 在此扩展
6. `.planning/phases/04-frontend-modernization-product-features/04-CONTEXT.md` § D-46/D-53/D-73 — HTMX HX-Request 分支 + Pico.css CDN(Phase 5 README 提示外网依赖)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 重灌生产数据 | entrypoint 无脑跑 `flask init-db`(Phase 2 D-15 是 drop+create+seed)会清生产数据 | advisory | Phase 5 D-75 引入 `--if-empty` flag,plan 阶段必须 verify Phase 2 init_db() 签名扩展且不破坏现有调用方 |
| docker-compose 含 MySQL 误导 | example yml 里写 mysql:8.0 service 会让读者误以为本项目打包 MySQL,违反 PROJECT.md Key Decision | advisory | D-80 已明确仅 app service;README 文字说明"MySQL 假设外部" |

## Infrastructure State

- Git: master 分支,本次 session 未 commit(uncommitted: 05-CONTEXT.md + 05-DISCUSSION-LOG.md + HANDOFF.json + 本文件)
- 此前已有 modified files:README.md / app/__init__.py / app/templates/*.html(Phase 4 残余,不归 Phase 5 处理)
- 无运行中 server / watcher

<context>
Phase 5 是产品化升级最后一站。Phase 1-4 已完成稳态:Python 3 + Flask 3 + mysqlclient/PyMySQL shim + Flask-WTF CSRF + admin 蓝图 + Comment 模型 + Pico.css/HTMX/Quill 前端。Phase 5 任务全是 deployment + test 层**新增**,不重构已交付代码。

用户审美:**资深运维画像 + 单 README.md 入口 + 最简 sh + root**。Planner 在 5.1 / 5.2 拆分时需保持此风格,避免过度工程化(不引入 K8s/CI/healthz/factory 重构等)。

讨论中我用了 6 个单问题完成 2 个区域,未选区域用 Claude 默认走(共 20 项 CD-XX),都已落 CONTEXT.md。本来还有 git commit + STATE.md update 两步,因上下文 80% 退出 — 这两步是机械操作,新 session 一开始即可补完。
</context>

<next_action>
**新 session 一开始的 3 个 bash 命令完成收尾:**

```bash
# 1. Commit 文档
git add .planning/phases/05-docker-deployment-tests-acceptance/ .planning/HANDOFF.json
git commit -m "docs(05): capture phase context for Docker Deployment, Tests & Acceptance"

# 2. STATE.md 更新(可选,gsd-plan-phase 会自动处理)
node "D:/work/baike/.claude/gsd-core/bin/gsd-tools.cjs" query state.record-session \
  --stopped-at "Phase 5 context gathered" \
  --resume-file ".planning/phases/05-docker-deployment-tests-acceptance/05-CONTEXT.md"

# 3. 进入规划
/gsd-plan-phase 5
```

或直接 `/gsd-plan-phase 5` — 规划器会读 CONTEXT.md(未提交也能读),只是 STATE.md 进度条不同步。
</next_action>
