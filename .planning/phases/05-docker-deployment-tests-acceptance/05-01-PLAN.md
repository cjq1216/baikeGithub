---
phase: 05-docker-deployment-tests-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/__init__.py
  - app/api/model.py
  - Dockerfile (new)
  - entrypoint.sh (new)
  - .dockerignore (new)
  - docker-compose.example.yml (new)
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08
  - INFRA-11
user_setup: []
must_haves:
  truths:
    - "第三方 `docker run baike:latest -e DB_HOST=... -e ...` 能在 30s 内启动并监听 :8000,首次自动建表+灌种子,容器重启后数据保留"
    - "`Dockerfile` 多阶段构建产物 < 200MB,`docker build` 输出 'Sending build context to Docker daemon' < 50MB"
    - "`/api/reset` 在镜像内不暴露(`app.debug=False` 走 abort 404,容器内默认 debug=False)"
    - "`FLASK_BEHIND_PROXY=true` 环境下 `url_for(_external=True)` 生成 https URL,`request.remote_addr` 显示 nginx 真实客户端 IP(非 127.0.0.1)"
    - "`flask init-db --if-empty` 在已有数据时秒返(无 drop_all),在空库时建表+灌种子"
  artifacts:
    - path: "Dockerfile"
      provides: "多阶段镜像构建(builder 装 mysqlclient 编译依赖 + runtime 仅 libmariadb3)"
      min_lines: 30
    - path: "entrypoint.sh"
      provides: "wait-for-mysql 30s + flask init-db --if-empty + exec gunicorn"
      min_lines: 10
      contains: "exec gunicorn"
    - path: ".dockerignore"
      provides: "排除 .git/.venv/.planning/tests/instance/.flask_secret,保留 README.md"
    - path: "docker-compose.example.yml"
      provides: "单 baike service + env 变量(不含 MySQL service)"
      contains: "services:"
    - path: "app/__init__.py"
      provides: "ProxyFix 条件启用块(env FLASK_BEHIND_PROXY=true 才 wrap)"
      contains: "ProxyFix"
  key_links:
    - from: "entrypoint.sh"
      to: "app/api/model.py:init_db"
      via: "flask init-db --if-empty (D-75/CD-36)"
      pattern: "flask init-db"
    - from: "Dockerfile"
      to: "app/__init__.py"
      via: "ENV FLASK_BEHIND_PROXY=true 触发 ProxyFix 自动启用 (D-88)"
      pattern: "FLASK_BEHIND_PROXY"
    - from: "docker-compose.example.yml"
      to: "app/__init__.py:DB URI 拼装"
      via: "env vars DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME (Phase 2 D-05/07)"
      pattern: "DB_HOST"
---

# Plan 5.1: Docker 化 (Dockerfile + entrypoint + .dockerignore + ProxyFix + init_db 扩展)

## Goal

第三方运维 clone 仓库后,执行 `docker build -t baike:latest .` 产出 < 200MB 镜像;`docker run -e DB_HOST=... ... baike:latest` 启动后,容器内 gunicorn 监听 0.0.0.0:8000,首次自动调 `flask init-db --if-empty` 建表+灌 7 条种子词条+admin 账号 `a`/`a`,容器重启数据保留,`/api/reset` 在生产模式被 404 屏蔽。

## Requirements Covered

- **INFRA-07** (Dockerfile multi-stage `python:3.11-slim` → gunicorn :8000)
- **INFRA-08** (entrypoint `flask init-db` 幂等 + 启动 gunicorn,`/api/reset` 在生产不暴露)
- **INFRA-11** (`.dockerignore` 构建上下文 < 50MB)

## NOT Covered (说明)

- **INFRA-09** (env vars DB_HOST/DB_USER/...) **已在 Phase 2 完成**,本 plan **不重复**(REQUIREMENTS.md 标记 Complete,ROADMAP 误标 5.1)。Plan 5.1 的 `docker-compose.example.yml` 复用 Phase 2 既有的 env var 拼装逻辑(`app/__init__.py:94-105`)。
- **TEST-01..05** (pytest + smoke + README) 归 Plan 5.2。
- **INFRA-10** (README 部署文档) 归 Plan 5.2。

## Locked Decisions Honored

- D-75 (init_db 加 `if_empty` 守卫 + CLI `--if-empty` flag)
- D-76 (wait-for-mysql 用 stdlib socket 重试 30s,零外部依赖)
- D-77 (entrypoint 走 sh + root,gunicorn 走 `exec` 收 PID 1)
- D-78/79/80 (README/docker-compose 留 Plan 5.2,本 plan 仅落地 compose 文件骨架)
- D-81/82/83 (Dockerfile multi-stage builder + runtime,WORKDIR /app,CMD entrypoint.sh)
- D-84 (.dockerignore 排除 .planning/tests/instance)
- D-87 (requirements.txt 加 gunicorn 一行,pytest/pytest-flask 留 Plan 5.2)
- D-88 (ProxyFix 条件启用 `if os.environ.get('FLASK_BEHIND_PROXY', '').lower() == 'true'`)
- CD-22 (ProxyFix x_for=1, x_proto=1, x_host=1)
- CD-24 (日志走 stdout)
- CD-31 (base image `python:3.11-slim`,不锁 patch)
- CD-33 (RUN chmod +x entrypoint.sh 兜底)
- CD-36 (init_db 向后兼容:`/api/reset` 路由和现有 `flask init-db` CLI 不传 `if_empty` 行为不变)
- CD-37 (Dockerfile **不**装 vim/curl)

## Tasks

<task type="auto">
  <name>Task 1: requirements.txt 加 gunicorn (D-87 一半)</name>
  <files>requirements.txt</files>
  <action>
    在 `requirements.txt` 文件末尾追加一行 `gunicorn>=21.0,<22.0  # 生产 WSGI server`(D-87 Phase 5 增量第一行)。**仅**加这一行,pytest/pytest-flask 留 Plan 5.2 再加。现有 7 行(Flask / Flask-Login / Flask-SQLAlchemy / mysqlclient / PyMySQL / Flask-WTF / bleach)不变。

    不要把 bleach 之前任何行重排(Phase 4 既有顺序保持不变)。**不**新增 `wait-for-it` / `gunicorn-paste` / `docker-compose` Python 库(D-87 显式排除)。
  </action>
  <verify>
    <automated>cat requirements.txt | grep -E '^gunicorn>=21' && pip install -r requirements.txt 2>&1 | tail -3 && gunicorn --version 2>&1 | head -1</automated>
  </verify>
  <done>
    - `requirements.txt` 含 8 行(原 7 行 + 新增 `gunicorn>=21.0,<22.0`)
    - `gunicorn --version` 输出含 "21."(或 22.x,具体随 wheel 决定)
    - 现有 7 行顺序与字符不变
  </done>
</task>

<task type="auto">
  <name>Task 2: init_db(if_empty=False) 函数签名扩展 (D-75)</name>
  <files>app/api/model.py</files>
  <action>
    改 `app/api/model.py:72` 的 `init_db()` 函数签名,加 `if_empty: bool = False` 参数(D-75)。函数顶部加守卫:

    ```python
    def init_db(if_empty: bool = False):
        if if_empty:
            try:
                if User.query.count() > 0:
                    return  # 已初始化,跳过 drop + create + seed
            except Exception:
                pass  # 表不存在(OperationalError),继续走 drop + create + seed
        db.drop_all()
        db.create_all()
        # ... 后续 User/Lemma 灌种子代码完全不变
    ```

    守卫顺序:`if_empty=True` 时先 `User.query.count()` 探测;**仅在 > 0 时 `return` 早退**;探测过程若表不存在(SQLAlchemy `OperationalError` 或 `ProgrammingError`),吞掉异常继续走 `drop_all` 重建。注意:不调 `db.create_all()` 在 if_empty 路径上(空库 / 首次启动时,SQLAlchemy `User.query.count()` 会因表不存在抛异常,被 except 捕获后,自然走下面 `drop_all` + `create_all` + seed;这正是 "首次启动自动建表" 的语义)。

    关键约束(CD-36 向后兼容):
    - 默认 `if_empty=False` → 行为**完全**等于改造前(直接 `drop_all` + `create_all` + seed)
    - `/api/reset` 路由(`app/api/__init__.py:160-168`)调 `init_db()` 不传参 → 不变
    - `flask init-db` CLI(见 Task 3)不传参时 → 不变
  </action>
  <verify>
    <automated>python -c "
from app import app, db
from app.api.model import init_db, User, Lemma
app.config['SQLALCHEMY_DATABASE_URI']='sqlite:///:memory:'
with app.app_context():
    db.create_all()
    init_db(if_empty=False); print('count after if_empty=False:', User.query.count())
    init_db(if_empty=True); print('count after if_empty=True:', User.query.count())
"</automated>
  </verify>
  <done>
    - `init_db()` 函数签名改为 `init_db(if_empty: bool = False)`
    - 函数顶部加 `if if_empty: try: User.query.count() > 0 → return except: pass` 守卫
    - 默认 `if_empty=False` 时 `db.drop_all() + create_all() + seed` 行为不变
    - `if_empty=True` 在已有数据时秒返(无 drop);在空库 / 表不存在时走 drop+create+seed
  </done>
</task>

<task type="auto">
  <name>Task 3: flask init-db CLI 加 --if-empty flag (D-75 另一半)</name>
  <files>app/__init__.py</files>
  <action>
    改 `app/__init__.py:139-144` 的 `init_db_command` 函数,加 `@click.option('--if-empty', is_flag=True)` 装饰器;函数体加 `if_empty: bool` 参数,透传给 `init_db(if_empty=if_empty)`。

    修改前(现状):
    ```python
    @app.cli.command("init-db")
    def init_db_command():
        """Drop all, recreate all, seed."""
        from app.api.model import init_db
        init_db()
        print('Database initialized.')
    ```

    修改后:
    ```python
    @app.cli.command("init-db")
    @click.option('--if-empty', is_flag=True, help='Skip re-init if User table already has rows.')
    def init_db_command(if_empty):
        """Drop all, recreate all, seed (idempotent with --if-empty)."""
        from app.api.model import init_db
        init_db(if_empty=if_empty)
        if if_empty:
            print('Database initialized (or skipped if non-empty).')
        else:
            print('Database initialized.')
    ```

    关键约束(CD-36 向后兼容):
    - 现有调用方 `/api/reset` 路由 + `flask init-db` 不传 flag 时,`if_empty` 默认 `False`,行为不变
    - entrypoint.sh(见 Task 6)调 `flask init-db --if-empty`,**仅**在容器启动时走幂等路径
    - 运维需要强制重灌:`docker exec ... flask init-db`(不带 flag)
  </action>
  <verify>
    <automated>FLASK_APP=run.py flask init-db --help 2>&1 | grep -A1 'if-empty' && python -c "from app import app; print('cli command registered:', any(c.name == 'init-db' for c in app.cli.commands.values()))"</automated>
  </verify>
  <done>
    - `flask init-db --help` 输出含 `--if-empty` flag 描述
    - `flask init-db --if-empty` 在已有数据时秒返(不 drop)
    - `flask init-db` 不带 flag 行为不变(强制重建)
  </done>
</task>

<task type="auto">
  <name>Task 4: app/__init__.py 加 ProxyFix 条件启用 (D-88)</name>
  <files>app/__init__.py</files>
  <action>
    在 `app/__init__.py:62-63` 之间(`app = Flask(__name__)` 与 `app.secret_key = _resolve_flask_secret()` 之间)加 ProxyFix 条件启用块。**位置选择理由**:ProxyFix wrap 在 secret_key 设置之后,db.init_app / blueprint register / CSRFProtect 之前,确保 middleware 在最外层生效。

    ```python
    app = Flask(__name__)

    # D-88 / CD-22: ProxyFix 条件启用,默认关闭(dev python run.py 不经代理,
    # 避免 X-Forwarded-For 伪报头被信任)。Dockerfile 设 ENV FLASK_BEHIND_PROXY=true
    # 触发自动启用,让 nginx 反代下 url_for(_external=True) 生成 https URL,
    # request.remote_addr 显示真实客户端 IP。trust 1 层代理(本项目无 CDN/LB)。
    if os.environ.get('FLASK_BEHIND_PROXY', '').lower() == 'true':
        from werkzeug.middleware.proxy_fix import ProxyFix
        app.wsgi_app = ProxyFix(app.wsgi_app, x_for=1, x_proto=1, x_host=1)

    app.secret_key = _resolve_flask_secret()
    ```

    关键约束:
    - 条件 `os.environ.get('FLASK_BEHIND_PROXY', '').lower() == 'true'` — 严格匹配小写 'true'(其他 '1' / 'yes' / 'True' 都不触发,避免环境变量拼写差异误启用)
    - trust 1 层代理(`x_for=1, x_proto=1, x_host=1` 三个参数)
    - 默认关闭(本地 `python run.py` 不设 env,ProxyFix 不 wrap)
    - 引用 `werkzeug.middleware.proxy_fix` 而非旧 `werkzeug.contrib.fixers.ProxyFix`(Flask 3.x / Werkzeug 2.x+ 路径)
  </action>
  <verify>
    <automated>FLASK_APP=run.py python -c "from app import app; print('default WSGI:', type(app.wsgi_app).__name__)" && FLASK_BEHIND_PROXY=true FLASK_APP=run.py python -c "from app import app; print('proxy WSGI:', type(app.wsgi_app).__name__)"</automated>
  </verify>
  <done>
    - 默认(无 env)→ `app.wsgi_app` 类型不含 `ProxyFix`
    - `FLASK_BEHIND_PROXY=true` → `app.wsgi_app` 类型含 `ProxyFix`
    - 不影响 dev 模式 `request.remote_addr`(X-Forwarded-* 头不被信任)
  </done>
</task>

<task type="auto">
  <name>Task 5: Dockerfile 多阶段构建 (D-81/82/83)</name>
  <files>Dockerfile (new)</files>
  <action>
    新建仓库根 `Dockerfile`(D-81/82/83),多阶段 builder + runtime。骨架:

    ```dockerfile
    # ---- builder stage: 装 mysqlclient C 扩展 + 全量 Python 依赖 ----
    FROM python:3.11-slim AS builder
    RUN apt-get update && apt-get install -y --no-install-recommends \
        build-essential pkg-config default-libmysqlclient-dev \
        && rm -rf /var/lib/apt/lists/*
    COPY requirements.txt .
    RUN pip install --user --no-cache-dir -r requirements.txt

    # ---- runtime stage: 仅运行时依赖 + 应用代码 ----
    FROM python:3.11-slim
    RUN apt-get update && apt-get install -y --no-install-recommends \
        libmariadb3 \
        && rm -rf /var/lib/apt/lists/*
    # D-82: pip --user 安装的 bin 在 /root/.local/bin
    ENV PATH=/root/.local/bin:$PATH \
        PYTHONDONTWRITEBYTECODE=1 \
        PYTHONUNBUFFERED=1 \
        FLASK_BEHIND_PROXY=true
    WORKDIR /app
    COPY --from=builder /root/.local /root/.local
    COPY app/ ./app/
    COPY run.py entrypoint.sh ./
    RUN chmod +x entrypoint.sh
    EXPOSE 8000
    # D-83: ENTRYPOINT 走 sh 脚本(需先 wait-for-mysql + init-db)
    CMD ["./entrypoint.sh"]
    ```

    关键约束(CD-37):
    - builder stage 装 `build-essential` + `pkg-config` + `default-libmysqlclient-dev`(mysqlclient C extension 编译)
    - runtime stage **只**装 `libmariadb3`(运行时 MySQL client 库,~8MB),**不**装 mariadb-client / vim / curl / wget(CD-37)
    - 两次 `apt-get update` 配 `--no-install-recommends` + 删 `/var/lib/apt/lists/*` 控体积
    - `ENV FLASK_BEHIND_PROXY=true` 让容器内 gunicorn 走 ProxyFix(D-88 + Task 4)
    - `PYTHONUNBUFFERED=1` 让 stdout 日志不被缓冲(CD-24 Docker 日志收集需要)
    - `EXPOSE 8000`(CD-21 gunicorn 端口)
    - `CMD ["./entrypoint.sh"]` 而非 `CMD ["gunicorn", ...]`(D-83,entrypoint 需先 wait-for-mysql + init-db)
  </action>
  <verify>
    <automated>test -f Dockerfile && head -3 Dockerfile && grep -E 'FROM python:3.11-slim' Dockerfile && grep -E 'FLASK_BEHIND_PROXY' Dockerfile && grep -E 'libmariadb3' Dockerfile && grep -E 'entrypoint.sh' Dockerfile && ! grep -E 'apt-get install.*vim|apt-get install.*curl' Dockerfile && echo 'all checks pass'</automated>
  </verify>
  <done>
    - `Dockerfile` 含 `FROM python:3.11-slim AS builder` 和 `FROM python:3.11-slim` 两段
    - builder 装 `build-essential pkg-config default-libmysqlclient-dev`
    - runtime 装 `libmariadb3`(**不**装 vim/curl)
    - `ENV FLASK_BEHIND_PROXY=true`
    - `CMD ["./entrypoint.sh"]`
    - `EXPOSE 8000`
  </done>
</task>

<task type="auto">
  <name>Task 6: entrypoint.sh wait-for-mysql + init-db --if-empty + gunicorn (D-76/77)</name>
  <files>entrypoint.sh (new)</files>
  <action>
    新建仓库根 `entrypoint.sh`(D-76 + D-77),sh 脚本 + chmod +x。内容:

    ```sh
    #!/bin/sh
    # D-76: 内联 Python wait-for-mysql,30s 重试,零外部依赖(stdlib socket)。
    # MySQL 未在 30s 内可连接则 exit 1,让 docker --restart 策略自动重试容器。
    set -e
    python -c "
    import os, socket, sys, time
    host = os.environ.get('DB_HOST', 'db')
    port = int(os.environ.get('DB_PORT', '3306'))
    deadline = time.time() + 30
    while time.time() < deadline:
        try:
            with socket.create_connection((host, port), timeout=1):
                sys.exit(0)
        except OSError:
            time.sleep(1)
    sys.stderr.write('MySQL %s:%s not reachable within 30s\n' % (host, port))
    sys.exit(1)
    "

    # D-75: 幂等初始化(已有数据则秒返)
    flask init-db --if-empty

    # D-77: exec 让 gunicorn 收 PID 1,SIGTERM 正常转发;日志走 stdout 给 Docker 收集
    exec gunicorn --bind 0.0.0.0:8000 --workers 2 --threads 4 --timeout 30 \
         --access-logfile - --error-logfile - \
         run:app
    ```

    关键约束:
    - `#!/bin/sh` 而非 `#!/bin/bash`(python:3.11-slim 默认 shell 是 dash 兼容 sh,D-77 节省 ~12MB 不装 bash)
    - 注释清楚区分 D-76 / D-75 / D-77 三段来源
    - `set -e` 任一命令失败立即退出(让 docker 看 non-zero 退出码)
    - `exec gunicorn ...` **最后一行**且**不**带 `&`(gunicorn 收 PID 1 接 SIGTERM)
    - workers=2 threads=4 timeout=30(CD-21)
    - `--access-logfile - --error-logfile -`(CD-24,Docker 日志走 stdout/stderr)
  </action>
  <verify>
    <automated>test -x entrypoint.sh && head -1 entrypoint.sh && grep -E 'flask init-db --if-empty' entrypoint.sh && grep -E 'exec gunicorn' entrypoint.sh && grep -E 'socket.create_connection' entrypoint.sh && grep -E 'workers 2' entrypoint.sh && echo 'all checks pass'</automated>
  </verify>
  <done>
    - `entrypoint.sh` 可执行权限(后续 RUN chmod +x + 本地 chmod +x)
    - 首行 `#!/bin/sh`
    - 含 `flask init-db --if-empty`
    - 含 `exec gunicorn` 收 PID 1
    - 含 `socket.create_connection` stdlib wait-for-mysql
    - `gunicorn ... --workers 2 --threads 4 --timeout 30 --access-logfile - --error-logfile -`
  </done>
</task>

<task type="auto">
  <name>Task 7: .dockerignore 排除 .git/.venv/.planning/tests/instance (D-84)</name>
  <files>.dockerignore (new)</files>
  <action>
    新建仓库根 `.dockerignore`(D-84),内容:

    ```
    .git
    .gitignore
    .gitattributes
    .venv
    venvbaike
    __pycache__
    *.pyc
    *.pyo
    .idea
    .vscode
    .claude
    .planning
    .pytest_cache
    tests/
    instance/.flask_secret
    *.md
    !README.md
    baike.sql
    config.ini
    .DS_Store
    Thumbs.db
    ```

    关键决策(D-84):
    - **`.planning/` 排除**(GSD 规划文档,部署不需要)
    - **`tests/` 排除**(测试在本地 / CI 跑,运行时不需要;Plan 5.2 跑 `make test` 用工作区源码)
    - **`instance/.flask_secret` 排除**(Phase 2 D-06 dev 临时密钥;生产必传 `-e FLASK_SECRET`)
    - **`!README.md` 保留 README**(运维 `docker run` 后 `docker exec cat /app/README.md` 可查)
    - **`baike.sql` / `config.ini` 排除**(uwsgi 历史 + 原始 SQL dump,Phase 5 部署不用)
    - `.DS_Store` / `Thumbs.db` macOS / Windows 残留
  </action>
  <verify>
    <automated>test -f .dockerignore && grep -E '^\.planning$' .dockerignore && grep -E '^tests/$' .dockerignore && grep -E '^\.flask_secret$|instance.*flask_secret' .dockerignore && grep -E '^!README\.md$' .dockerignore && grep -E '^\.git$' .dockerignore && echo 'all checks pass'</automated>
  </verify>
  <done>
    - `.dockerignore` 排除 `.git` / `.venv` / `.planning` / `tests/` / `instance/.flask_secret` / `baike.sql` / `config.ini`
    - 保留 README.md(`!README.md` 倒数规则)
    - 排除 `__pycache__` / `*.pyc` / `.idea` / `.vscode` / `.claude`
  </done>
</task>

<task type="auto">
  <name>Task 8: docker-compose.example.yml 仅 app service (D-80)</name>
  <files>docker-compose.example.yml (new)</files>
  <action>
    新建仓库根 `docker-compose.example.yml`(D-80),仅含 baike app service,**不**含 MySQL / nginx。内容:

    ```yaml
    # 互动百科 (baike) — app 容器示例
    # MySQL 与 nginx 假定由外部基础设施提供,本 compose 仅展示 baike app 启动方式
    # 使用方法:
    #   1. 复制本文件为 docker-compose.yml: cp docker-compose.example.yml docker-compose.yml
    #   2. 创建 .env 文件并填入 DB_PASSWORD / FLASK_SECRET:
    #        echo 'DB_PASSWORD=your_mysql_password' > .env
    #        echo 'FLASK_SECRET=$(openssl rand -hex 32)' >> .env
    #   3. docker compose up -d
    services:
      baike:
        image: baike:latest
        build:
          context: .
          dockerfile: Dockerfile
        ports:
          - "8000:8000"
        environment:
          DB_HOST: db.internal.example.com
          DB_PORT: "3306"
          DB_USER: baike_user
          DB_PASSWORD: ${DB_PASSWORD}
          DB_NAME: baike
          FLASK_SECRET: ${FLASK_SECRET}
        restart: always
    ```

    关键约束(D-80 + CD-39):
    - **不**含 `mysql:8.0` service / volume / healthcheck
    - **不**含 nginx service / config
    - `DB_PASSWORD` / `FLASK_SECRET` 用 `${VAR}` 占位,README(Plan 5.2 T6)说明创建 `.env` 方式(CD-39)
    - 其他 env vars 给出示例值(`db.internal.example.com` / `baike_user` / `baike`),运维按需改
    - `restart: always` 让异常退出的容器自动重启
    - `build: context: .` 让 `docker compose build` 也可工作,不只 `docker run` 外部 image
  </action>
  <verify>
    <automated>test -f docker-compose.example.yml && grep -E '^services:' docker-compose.example.yml && grep -E 'image: baike' docker-compose.example.yml && grep -E 'DB_PASSWORD: \$\{DB_PASSWORD\}' docker-compose.example.yml && grep -E 'FLASK_SECRET: \$\{FLASK_SECRET\}' docker-compose.example.yml && ! grep -E 'mysql:8\.0|image: mysql' docker-compose.example.yml && ! grep -E 'image: nginx' docker-compose.example.yml && echo 'all checks pass'</automated>
  </verify>
  <done>
    - `docker-compose.example.yml` 含 `services:` 块
    - 唯一 service 是 `baike:`
    - 6 个 env vars:`DB_HOST` / `DB_PORT` / `DB_USER` / `DB_PASSWORD` / `DB_NAME` / `FLASK_SECRET`
    - `${DB_PASSWORD}` / `${FLASK_SECRET}` 占位
    - **不**含 `mysql:8.0` / `image: nginx`
  </done>
</task>

<verification>
完成 Plan 5.1 后,做以下静态 + import 验证:

1. **requirements.txt 增量** (D-87):
   - `grep -c '^' requirements.txt` 输出 8
   - `grep -E '^gunicorn>=21' requirements.txt` 退出 0
   - `gunicorn --version` 输出 21.x 或 22.x

2. **init_db 向后兼容** (D-75 / CD-36):
   - `python -c "from app.api.model import init_db; import inspect; print(inspect.signature(init_db))"` 输出 `(if_empty: bool = False)`
   - 现有 `flask init-db`(无 flag)行为不变(强制重建)
   - 现有 `GET /api/reset`(dev mode)行为不变

3. **flask init-db --if-empty** (D-75 另一半):
   - `FLASK_APP=run.py flask init-db --help` 输出含 `--if-empty`
   - `flask init-db --if-empty` 在已有数据时秒返(只 print 不 drop)

4. **ProxyFix 条件启用** (D-88 / CD-22):
   - 默认:`python -c "from app import app; print(type(app.wsgi_app).__name__)"` 输出 `Flask`
   - 设 env:`FLASK_BEHIND_PROXY=true python -c "from app import app; print(type(app.wsgi_app).__name__)"` 输出含 `ProxyFix`

5. **Dockerfile 形态** (D-81/82/83):
   - `grep -c '^FROM' Dockerfile` 输出 2
   - `grep -E 'libmariadb3' Dockerfile` 退出 0
   - `grep -E 'FLASK_BEHIND_PROXY' Dockerfile` 退出 0
   - `! grep -E 'apt-get install.*vim|apt-get install.*curl' Dockerfile`

6. **entrypoint.sh 形态** (D-76/77):
   - `test -x entrypoint.sh`
   - `head -1 entrypoint.sh` 输出 `#!/bin/sh`
   - `grep -E 'flask init-db --if-empty' entrypoint.sh` 退出 0
   - `grep -E 'exec gunicorn.*workers 2' entrypoint.sh` 退出 0
   - `grep -E 'socket.create_connection' entrypoint.sh` 退出 0

7. **.dockerignore 完整性** (D-84):
   - `grep -c '^.git$\|^.venv$\|^.planning$\|^tests/$' .dockerignore` ≥ 4
   - `grep -E '^!README\.md$' .dockerignore` 退出 0

8. **docker-compose.example.yml 形态** (D-80):
   - `grep -E '^services:' docker-compose.example.yml` 退出 0
   - `grep -E 'image: baike' docker-compose.example.yml` 退出 0
   - `! grep -E 'image: mysql|image: nginx' docker-compose.example.yml`

**实际 docker build / docker run / docker compose 验证在 execute 阶段做**(plan-checker 不跑 docker,任务 verify 命令只检查文件存在 + 内容 grep)。
</verification>

<success_criteria>
- INFRA-07, INFRA-08, INFRA-11 三条需求对应的 Dockerfile / entrypoint / .dockerignore 文件就位
- 现有 Phase 1-4 代码 0 重构(`app/api/__init__.py` 路由 / 模板 / routes / 模型 schema 全部不变;`init_db()` 与 `flask init-db` 默认行为不变)
- 所有 8 个任务 verify 命令通过(grep / 文件存在 / 签名检查)
- 第三方 clone 仓库后可执行 `docker build` + `docker run` 完成本地起容器流程(执行阶段验证)
</success_criteria>

<output>
完成 Plan 5.1 全部 8 个任务后,创建 `.planning/phases/05-docker-deployment-tests-acceptance/05-01-SUMMARY.md`(由 execute-phase 流程生成)
</output>
