---
phase: 05-docker-deployment-tests-acceptance
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - requirements.txt
  - tests/conftest.py (new)
  - tests/test_smoke.py (new)
  - tests/test_admin.py (new)
  - Makefile (new)
  - README.md (rewrite)
autonomous: true
requirements:
  - INFRA-10
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
user_setup: []
must_haves:
  truths:
    - "`make test` 在 fresh venv(SQLite in-memory)上全绿,9 步端到端流程 + admin 删 + 非 admin 403 全部通过"
    - "`make smoke` 单独跑 test_smoke.py 也能全绿"
    - "README 第三方可独立执行:`docker run` + nginx 反代 + smoke flow 9 步手工验收清单,无 FAIL 步骤"
    - "test_admin.py 验证 admin `a`/`a` 既能删任意 lemma + 级联删 comments,也能删任意 comment;非 admin `bob` POST `/api/admin/lemma/.../delete` 和 `/api/admin/comment/.../delete` 返 403"
    - "test_smoke.py 9 步每步 assert:302 redirect + 副作用落地(用户存在/词条存在/评论存在/被删/登出)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "client fixture (SQLite in-memory + WTF_CSRF_ENABLED=False + init_db seed)"
      contains: "from app import app, db"
    - path: "tests/test_smoke.py"
      provides: "test_full_user_flow 9 步端到端单测 (D-85)"
      contains: "def test_full_user_flow"
    - path: "tests/test_admin.py"
      provides: "test_admin_can_delete_any + test_non_admin_forbidden (D-86)"
      contains: "admin_required"
    - path: "Makefile"
      provides: "make test / make smoke / make docker-build / make docker-run targets (CD-30/32)"
      contains: "test:"
    - path: "README.md"
      provides: "150 行资深运维向:本地开发 + 生产部署 + nginx snippet + pytest + 9 步 smoke 验收清单 + FAQ"
      min_lines: 100
      contains: "## 验收清单"
  key_links:
    - from: "tests/conftest.py:client fixture"
      to: "app/__init__.py:app"
      via: "from app import app, db (CD-26 module-level,非 factory 重构)"
      pattern: "from app import app"
    - from: "tests/test_smoke.py:9 步流程"
      to: "app/api/__init__.py + app/route/user.py"
      via: "client.post / client.get 标准 test_client 调用 (CD-25 走整页 302 redirect,不覆盖 HX-Request)"
      pattern:client.post.*api/
    - from: "tests/test_admin.py"
      to: "app/api/admin.py:delete_lemma + delete_comment"
      via: "admin 登录后 POST /api/admin/lemma/<id>/delete + /api/admin/comment/<id>/delete"
      pattern:api/admin/
    - from: "README.md"
      to: "Plan 5.1 Dockerfile/entrypoint/compose"
      via: "docker run 示例 + nginx 反代 + smoke flow 验证步骤"
      pattern:docker run
---

# Plan 5.2: pytest smoke 测试 + Makefile + README 验收文档

## Goal

第三方运维 fresh venv `pip install -r requirements.txt` 后,`make test` 全绿(9 步端到端 smoke + admin 删 + 非 admin 403 三组测试),`make smoke` 单独跑 smoke 也全绿;README 150 行涵盖本地开发、Docker 生产部署(docker run + nginx 反代 snippet)、pytest 跑法、9 步手工 smoke 验收清单,第三方"按 README 复制粘贴"即可完成端到端验收。

## Requirements Covered

- **INFRA-10** (README "Production deploy" 段:build image + docker run env + nginx 反代 + 外部 MySQL,第三方可独立 follow)
- **TEST-01** (pytest + pytest-flask 装好,tests/ 目录存在,至少含 smoke + admin)
- **TEST-02** (test_smoke.py 单测 9 步:注册→登录→建词条→搜索→看详情→编辑→评论→删自己评论→登出)
- **TEST-03** (test_admin.py:admin 删 lemma + 删 comment;非 admin 403)
- **TEST-04** (make test 在 fresh venv 通过;README 文档化如何跑)
- **TEST-05** (README 内嵌 9 步手工验收清单,第三方 docker run 后能照着走)

## Depends On

- **Plan 5.1**(Dockerfile/entrypoint/init_db 扩展/ProxyFix 条件启用/requirements.txt gunicorn)必须先完成。本 plan 直接消费其产物:
  - `requirements.txt` 已有 `gunicorn`,本 plan 末尾追加 2 行 `pytest` + `pytest-flask`
  - `app/__init__.py:init_db(if_empty)` 可在测试 fixture 中以 `if_empty=False` 强制重建
  - `app/__init__.py:ProxyFix` 条件启用,测试不设 `FLASK_BEHIND_PROXY` 走默认关闭路径

## NOT Covered

- **HTMX HX-Request 分支 smoke 覆盖**(CD-29 明确留手工验收)
- **CI / K8s / 监控 / 容器镜像签名 / 多架构**(V2-OPS-01..03,Deferred Ideas)
- **MySQL 容器集成测试**(CD-25 选 SQLite in-memory,V2 deferred)
- **pytest-cov 覆盖率工具**(D-87 显式排除)
- **Application Factory 重构**(CD-26 显式排除)

## Locked Decisions Honored

- D-78 (README 资深运维向,~150 行,默认 docker run + nginx snippet + smoke 验收清单)
- D-79 (TEST-05 验收清单全进 README,无 ACCEPTANCE.md / DEPLOY.md)
- D-80 (compose 仅 app service,MySQL/nginx 假定外部基础设施)
- D-85 (smoke 9 步单测序列)
- D-86 (admin 测 admin 可删 + 非 admin 403 两组)
- D-87 (requirements.txt 加 pytest + pytest-flask 两行)
- CD-25 (SQLite in-memory + WTF_CSRF_ENABLED=False)
- CD-26 (module-level Flask 实例直接 `from app import app, db`,无 factory 重构)
- CD-27 (CSRF 测试中关闭)
- CD-28 (test_smoke.py + test_admin.py 分离,共享 conftest.py)
- CD-29 (HTMX 路径 smoke 不覆盖)
- CD-30 (make test / make smoke 走 pytest 命令)
- CD-32 (Makefile target 命名 make test / make smoke / make docker-build / make docker-run)
- CD-35 (conftest.py 放 tests/conftest.py,**不**放 tests/__init__.py)
- CD-38 (README "已知限制 / FAQ" 章节列 4 条:MySQL 不打包 / FLASK_SECRET 必传 / 容器重启不清数据 / HTMX 走 jsdelivr CDN)
- CD-39 (compose 用 ${VAR} 占位,README 提示 .env 创建方式)
- CD-40 (TEST-05 验收清单纯人类 markdown,无 BDD 框架)

## Tasks

<task type="auto">
  <name>Task 1: requirements.txt 加 pytest + pytest-flask (D-87 二半)</name>
  <files>requirements.txt</files>
  <action>
    在 `requirements.txt` 文件末尾追加 2 行(D-87 Phase 5 增量第二半):

    ```
    pytest>=8.0,<9.0       # 测试框架
    pytest-flask>=1.3,<2.0 # Flask 测试辅助
    ```

    现有 8 行(Plan 5.1 Task 1 后的 8 行)不变。**不**新增 `pytest-cov`(D-87 显式排除,smoke 不需要覆盖率)。**不**重排既有行。

    注释文字采用 `# 测试框架` / `# Flask 测试辅助` 风格(D-87 示例注释)。
  </action>
  <verify>
    <automated>cat requirements.txt | grep -E '^pytest>=8' && cat requirements.txt | grep -E '^pytest-flask>=1\.3' && pip install -r requirements.txt 2>&1 | tail -3 && pytest --version 2>&1 | head -1</automated>
  </verify>
  <done>
    - `requirements.txt` 含 10 行(Plan 5.1 后 8 行 + 新增 pytest + pytest-flask)
    - `pytest --version` 输出 pytest 8.x
    - `pip show pytest-flask` 退出 0
  </done>
</task>

<task type="auto">
  <name>Task 2: tests/conftest.py — SQLite in-memory client fixture (CD-25/26/35)</name>
  <files>tests/conftest.py (new)</files>
  <action>
    新建 `tests/__init__.py`(空文件,让 `tests/` 成为 Python 包,pytest 跨模块 import 通畅)。

    新建 `tests/conftest.py`(CD-25/26/35),内容骨架:

    ```python
    # tests/conftest.py
    # CD-26: module-level Flask 实例(app/__init__.py:62)直接 import,不走 factory。
    # CD-25: SQLite in-memory + WTF_CSRF_ENABLED=False + TESTING=True,无需 MySQL。
    # CD-35: fixture 放 tests/conftest.py(社区惯例),不放 tests/__init__.py。
    import os
    import pytest

    # 给 DB_* env vars 占位(从 app/__init__.py 严格 require 改成可在测试 sqlite 下不依赖)
    # app/__init__.py 在 import 时会检查 DB_HOST 等;我们直接覆盖 SQLALCHEMY_DATABASE_URI。
    os.environ.setdefault('DB_HOST', 'localhost')
    os.environ.setdefault('DB_PORT', '3306')
    os.environ.setdefault('DB_USER', 'test')
    os.environ.setdefault('DB_PASSWORD', 'test')
    os.environ.setdefault('DB_NAME', 'test')

    @pytest.fixture
    def app():
        from app import app as flask_app, db
        flask_app.config.update(
            TESTING=True,
            WTF_CSRF_ENABLED=False,  # CD-27
            SQLALCHEMY_DATABASE_URI='sqlite:///:memory:',
        )
        with flask_app.app_context():
            db.create_all()
            from app.api.model import init_db
            init_db(if_empty=False)  # 灌 admin 'a'/'a' + 7 条种子 lemma
            yield flask_app
            db.session.remove()
            db.drop_all()

    @pytest.fixture
    def client(app):
        return app.test_client()
    ```

    关键约束:
    - **不**引入 application factory(CD-26)
    - **不**用 `pytest-flask` 的 `client` fixture(本项目 module-level app,直接包 `app.test_client()`)
    - fixture scope = function(想法 C:每个测试独立 DB 状态)
    - `init_db(if_empty=False)` 强制重建 + 灌 admin 种子
    - 5 个 env vars 占位(D-05 require)→ `setdefault` 不覆盖已有
  </action>
  <verify>
    <automated>test -f tests/conftest.py && test -f tests/__init__.py && python -c "
import os
os.environ.setdefault('DB_HOST','localhost'); os.environ.setdefault('DB_PORT','3306')
os.environ.setdefault('DB_USER','test'); os.environ.setdefault('DB_PASSWORD','test')
os.environ.setdefault('DB_NAME','test')
import pytest
exit_code = pytest.main(['-v', 'tests/conftest.py', '--collect-only'])
" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `tests/__init__.py` 存在(可为空)
    - `tests/conftest.py` 含 `app` + `client` fixture
    - `app` fixture 设 `SQLALCHEMY_DATABASE_URI='sqlite:///:memory:'` + `WTF_CSRF_ENABLED=False` + `TESTING=True`
    - `client` fixture 返回 `app.test_client()`
    - pytest collect-only 不报 fixture 错误
  </done>
</task>

<task type="auto">
  <name>Task 3: tests/test_smoke.py — 9 步端到端单测 (D-85)</name>
  <files>tests/test_smoke.py (new)</files>
  <action>
    新建 `tests/test_smoke.py`(D-85),单 `test_full_user_flow` 函数走 9 步流程。骨架:

    ```python
    # tests/test_smoke.py
    # D-85: 9 步端到端流程(单个 test function,想法 G:贴近"single test"措辞)
    # 1. POST /api/regist
    # 2. POST /api/login
    # 3. POST /api/add
    # 4. GET  /user/search
    # 5. GET  /user/detail (view_count + 1)
    # 6. POST /api/modify
    # 7. POST /api/comment
    # 8. POST /api/comment/<id>/delete (作者)
    # 9. POST /api/logout
    def test_full_user_flow(client):
        from app.api.model import User, Lemma, Comment, db

        # 步骤 1:注册 testuser(>6 字符)
        r = client.post('/api/regist', data={'name': 'testuser', 'password': 'testpass123'})
        assert r.status_code == 302
        with client.session_transaction() as sess:
            assert '_user_id' in sess  # login_user 设了 session
        # 副作用:用户存在
        # (db 在 app fixture 内,直接 query 当前 app context)

        # 步骤 2:登出后再登录
        client.get('/api/logout')
        r = client.post('/api/login', data={'name': 'testuser', 'password': 'testpass123'})
        assert r.status_code == 302

        # 步骤 3:添加词条
        r = client.post('/api/add', data={
            'title': '测试词条',
            'content': '测试内容 [[词条 A]]',
        })
        assert r.status_code == 302

        # 步骤 4:搜索
        r = client.get('/user/search?q=测试')
        assert r.status_code == 200
        assert b'\xe6\xb5\x8b\xe8\xaf\x95' in r.data or b'测试' in r.data  # UTF-8

        # 步骤 5:看详情
        r = client.get('/user/detail?title=测试词条')
        assert r.status_code == 200
        # D-85 断言:view_count 从 0 变 1
        # (此断言在 Phase 4 D-48 后才能稳定)

        # 步骤 6:编辑
        r = client.post('/api/modify', data={
            'newTitle': '测试词条',
            'newContent': '改后',
        })
        assert r.status_code == 302

        # 步骤 7:发评论
        r = client.post('/api/comment', data={
            'lemma_id': '1',
            'content': '测试评论',
        })
        assert r.status_code == 302

        # 步骤 8:作者删自己评论
        # (此时 Comment 表里应有 step 7 那条,id=1)
        r = client.post('/api/comment/1/delete')
        assert r.status_code == 302

        # 步骤 9:登出
        r = client.get('/api/logout')
        assert r.status_code == 302
        with client.session_transaction() as sess:
            assert '_user_id' not in sess
    ```

    关键约束:
    - **不**带 `HX-Request` header(CD-29 走整页 302)
    - **不**带 `csrf_token`(CD-27 WTF_CSRF_ENABLED=False)
    - `testuser` 长度 ≥ 6 字符(Phase 2 D-04 校验)
    - `client.session_transaction()` 验证 Flask-Login session(`_user_id` key)
    - view_count 断言需要 fixture 内有 `app.app_context()` — 步骤 5 内嵌 `with app.app_context():` 块 query
  </action>
  <verify>
    <automated>pytest -v tests/test_smoke.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `test_full_user_flow` 单测 9 步全过
    - 每步 assert `status_code == 302` 或 `200`
    - 副作用:用户存在 / lemma 存在 / comment 存在 / comment 被删 / session 清
    - pytest 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 4: tests/test_admin.py — admin 可删 + 非 admin 403 (D-86)</name>
  <files>tests/test_admin.py (new)</files>
  <action>
    新建 `tests/test_admin.py`(D-86),含两个 test function。骨架:

    ```python
    # tests/test_admin.py
    # D-86: admin 删 + 非 admin 403
    def test_admin_can_delete_any(client):
        # admin 'a'/'a' 登录(init_db 已 seed)
        r = client.post('/api/login', data={'name': 'a', 'password': 'a'})
        assert r.status_code == 302

        # 普通用户 bob 注册 + 创建 lemma + 评论
        client.get('/api/logout')
        client.post('/api/regist', data={'name': 'bobuser', 'password': 'bobpass123'})
        client.post('/api/add', data={'title': 'bob 词条', 'content': '<p>bob 内容</p>'})
        client.post('/api/comment', data={'lemma_id': '8', 'content': 'bob 评论'})  # lemma 7 是 seed 最后一个

        # admin 重新登录
        client.get('/api/logout')
        r = client.post('/api/login', data={'name': 'a', 'password': 'a'})
        assert r.status_code == 302

        # admin 删 lemma 8(级联删其下评论)
        r = client.post('/api/admin/lemma/8/delete')
        assert r.status_code == 302

        # admin 删另一条 comment
        # (需要在 bob 还在时再建一条 comment;但 lemma 8 已被删,改用 lemma 1)
        # 此处简化:lemma 8 删后,该 lemma 下 comment 也被级联删;另建 lemma 9 测 comment 删除
        # (或:此 test 留出 2 个 admin delete 调用覆盖两条路径即可)
        # 简化方案:lemma 1(seed '123')下加一条新评论,再 admin 删
        client.get('/api/logout')
        client.post('/api/login', data={'name': 'bobuser', 'password': 'bobpass123'})
        # bob 在 lemma 1 下发评论
        # 找到 bob 的 user id
        r = client.post('/api/comment', data={'lemma_id': '1', 'content': 'bob 在 lemma 1 的评论'})

        client.get('/api/logout')
        client.post('/api/login', data={'name': 'a', 'password': 'a'})
        # 找到 bob 那条 comment 的 id(简化:list all comments for lemma 1)
        r = client.get('/user/detail?title=123')
        # 解析 r.data 找 comment id ...
        # (此处用 SQLAlchemy query 简化,executor 写时用 db.session 直接 query)

        # admin 删 comment
        r = client.post('/api/admin/comment/<id>/delete')
        assert r.status_code == 302

    def test_non_admin_forbidden(client):
        # bob 登录
        r = client.post('/api/regist', data={'name': 'bobuser2', 'password': 'bobpass123'})
        assert r.status_code == 302

        # bob 尝试 admin 删 lemma → 403
        r = client.post('/api/admin/lemma/1/delete')
        assert r.status_code == 403

        # bob 尝试 admin 删 comment → 403
        r = client.post('/api/admin/comment/1/delete')
        assert r.status_code == 403
    ```

    关键约束(CD-25/26/27):
    - **不**带 `csrf_token`(CSRF 关)
    - **不**带 `HX-Request` header
    - admin 走 init_db seed 的 `a`/`a`(`is_admin=True`)
    - 非 admin 走 `bobuser` / `bobpass123`(Phase 2 D-04 长度 ≥ 6)
    - lemma id 数字假设(若 seed 数 7 条,id 1-7;bob 建的 lemma 拿 id 8;此处 executor 实施时根据 app fixture 内 db 实际情况调整)
  </action>
  <verify>
    <automated>pytest -v tests/test_admin.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `test_admin_can_delete_any` 全过:admin 登录 + 删 lemma + 删 comment
    - `test_non_admin_forbidden` 全过:bob 登录 + POST /api/admin/lemma/.../delete 返 403 + POST /api/admin/comment/.../delete 返 403
    - pytest 退出码 0
  </done>
</task>

<task type="auto">
  <name>Task 5: Makefile — make test / make smoke / make docker-build / make docker-run (CD-30/32)</name>
  <files>Makefile (new)</files>
  <action>
    新建仓库根 `Makefile`(CD-30/32),4 个 target。骨架:

    ```makefile
    # 互动百科 (baike) Makefile
    # CD-30/32: smoke tests + docker 便捷命令

    .PHONY: test smoke docker-build docker-run help

    help:
    	@echo "Available targets:"
    	@echo "  make test         — Run full test suite (smoke + admin)"
    	@echo "  make smoke        — Run smoke test only (tests/test_smoke.py)"
    	@echo "  make docker-build — Build baike:latest image"
    	@echo "  make docker-run   — Run container (requires DB_* env vars)"

    test:
    	pytest -v tests/

    smoke:
    	pytest -v tests/test_smoke.py

    docker-build:
    	docker build -t baike:latest .

    docker-run:
    	docker run --rm -p 8000:8000 \
    		-e DB_HOST=$$DB_HOST \
    		-e DB_PORT=$${DB_PORT:-3306} \
    		-e DB_USER=$$DB_USER \
    		-e DB_PASSWORD=$$DB_PASSWORD \
    		-e DB_NAME=$${DB_NAME:-baike} \
    		-e FLASK_SECRET=$$FLASK_SECRET \
    		baike:latest
    ```

    关键约束:
    - 4 个 target:`test` / `smoke` / `docker-build` / `docker-run`(CD-32 命名)
    - `make test` 走 `pytest -v tests/`(CD-30 跑全部)
    - `make smoke` 走 `pytest -v tests/test_smoke.py`(只跑 smoke)
    - `make docker-build` 调 `docker build -t baike:latest .`(复用 Plan 5.1 Dockerfile)
    - `make docker-run` 调 `docker run` 把 6 个 env vars 传给容器(运维在 shell export 后 `make docker-run`)
    - `help` target 给首次使用的运维一个入口
    - 用 `pytest` 而非 `python -m pytest`(CD-30 单行 command)
    - **不**引入 test category(unit/integration)区分(CD-30 注释:Phase 5 demo 性质无价值)
  </action>
  <verify>
    <automated>test -f Makefile && grep -E '^test:' Makefile && grep -E '^smoke:' Makefile && grep -E '^docker-build:' Makefile && grep -E '^docker-run:' Makefile && grep -E 'pytest -v tests/' Makefile && echo 'all checks pass' && make help 2>&1 | head -5</automated>
  </verify>
  <done>
    - `Makefile` 含 4 个 target:`test` / `smoke` / `docker-build` / `docker-run`
    - `make test` 跑 `pytest -v tests/`
    - `make smoke` 跑 `pytest -v tests/test_smoke.py`
    - `make docker-build` 跑 `docker build -t baike:latest .`
    - `make docker-run` 把 6 env vars 透传给容器
  </done>
</task>

<task type="auto">
  <name>Task 6: README.md 重写(150 行资深运维 + TEST-05 验收清单)(D-78/79/INFRA-10/TEST-05)</name>
  <files>README.md (rewrite)</files>
  <action>
    **完全重写** 仓库根 `README.md`(D-78/79/INFRA-10/TEST-05),目标 ~150 行,资深运维向。结构(D-79 锁):

    ```markdown
    # 互动百科 (baike)

    一个 Flask + MySQL 的小型互动百科。v1 产品化升级目标:第三方按本文档
    即可拉起 Docker 容器、注册账号、创建词条、发布评论、admin 端到端走一遍。

    ## 项目简介

    - 5 个 POST 接口(注册/登录/登出/加词条/改词条) + 3 个评论端点 + 2 个 admin 端点
    - 持久层:MySQL(外部,容器假定已就绪)
    - 前端:Pico.css + HTMX + Quill 富文本(CDN 加载)
    - 测试:pytest 端到端 smoke + admin 权限测试

    ## 本地开发

    ```bash
    # 1. 准备 MySQL(假定 root@localhost:3306 / 库名 baike 已建好)
    # 2. 准备虚拟环境
    python -m venv venvbaike
    source venvbaike/bin/activate     # Windows: venvbaike\Scripts\activate
    pip install -r requirements.txt

    # 3. 设 env vars(下面 6 个必填)
    export DB_HOST=localhost
    export DB_PORT=3306
    export DB_USER=root
    export DB_PASSWORD=your_password
    export DB_NAME=baike
    export FLASK_SECRET=$(openssl rand -hex 32)  # 生产必传,dev 可省(自动生成临时密钥)

    # 4. 初始化 DB
    flask init-db   # 强制重建 + 灌 admin 'a'/'a' + 7 条种子词条

    # 5. 启动
    python run.py   # 监听 5000 端口
    ```

    浏览器打开 http://127.0.0.1:5000/ → 注册 / 登录 / 加词条。

    ## 生产部署(Docker)

    ### 前置条件

    - Docker 20.10+
    - MySQL 5.7+ / 8.0 实例可达(外部基础设施,本项目不打包 MySQL)
    - nginx 反代(外部基础设施,本项目不打包 nginx)

    ### 环境变量(6 个必填)

    | 变量 | 说明 | 示例 |
    |------|------|------|
    | `DB_HOST` | MySQL 主机 | `db.internal.example.com` |
    | `DB_PORT` | MySQL 端口 | `3306` |
    | `DB_USER` | MySQL 用户 | `baike_user` |
    | `DB_PASSWORD` | MySQL 密码 | (从 secrets manager 取) |
    | `DB_NAME` | MySQL 库名 | `baike` |
    | `FLASK_SECRET` | Flask session 签名密钥 | `$(openssl rand -hex 32)` |

    **生产必须传 `FLASK_SECRET`**(容器重启后 dev 临时密钥会丢,导致全员 session 失效)。

    ### 启动

    ```bash
    # 1. 准备 .env 文件
    cat > .env <<EOF
    DB_PASSWORD=your_mysql_password
    FLASK_SECRET=$(openssl rand -hex 32)
    EOF

    # 2. 复制 compose example
    cp docker-compose.example.yml docker-compose.yml

    # 3. 改 docker-compose.yml 里 DB_HOST / DB_USER / DB_NAME 三个非占位 env vars

    # 4. 拉起容器
    docker compose up -d

    # 5. (首次)提升 admin(默认 'a'/'a' 已是 admin,无须提升)
    # 后续新增用户转 admin:docker compose exec baike flask promote-admin <username>

    # 6. 验证
    curl http://localhost:8000/user/home
    ```

    ### nginx 反代示例(~30 行 server block)

    ```nginx
    server {
        listen 80;
        server_name baike.example.com;
        location / {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    ```

    完整 nginx 主配置(events / http / upstream blocks)由运维侧负责,本项目仅给 server 块。

    ## 测试(pytest)

    ```bash
    make test      # 跑全部:smoke + admin
    make smoke     # 只跑 9 步端到端
    ```

    SQLite in-memory,无需 MySQL 容器;每个测试函数独立 DB 状态。

    ## 验收清单(smoke flow for third-party verification)

    第三方 fresh checkout + docker compose up -d 后,在浏览器走完以下 9 步:

    1. 打开 http://baike.example.com/ → 看到首页 + 已有 7 条种子词条
    2. 注册新账号(用户名 ≥ 6 字符 / 密码 ≥ 6 字符)→ 302 → 首页 + 导航栏右上角显示用户名
    3. 登出 → 重新登录(同账号)→ 302 → 首页
    4. 进入「写词条」→ 填 title + Quill 富文本(含 `[[wiki 链接]]` 语法)→ 提交 → 302 → 首页
    5. 搜索刚加的词条(首页搜索框)→ 看到列表
    6. 打开词条详情页 → 看到内容 + `[[wiki 链接]]` 渲染为蓝链接 / 红虚线(若目标不存在)+ 浏览数 + 1
    7. 点「修改」→ Quill 预填 + 改内容 + 提交 → 302 → 首页
    8. 词条详情页底部评论框发评论 → 看到评论显示在列表顶部
    9. 登出 / 重新登录 admin(`a`/`a`)→ 进任一词条详情页 → 看到「删除词条」按钮 + 点「删除」该词条下所有评论一并消失

    走完 9 步无 FAIL = v1 验收通过。

    ## 已知限制 / FAQ

    1. **本项目不打包 MySQL**:`docker-compose.example.yml` 仅含 baike app;MySQL / nginx 假定由外部基础设施提供。
    2. **`FLASK_SECRET` 生产必传**:容器重启 dev 临时密钥会丢;生产用 `openssl rand -hex 32` 生成。
    3. **容器重启数据保留**:entrypoint 走 `flask init-db --if-empty`,已有数据时秒返(不 drop)。要强制重灌: `docker compose exec baike flask init-db`(不带 flag)。
    4. **HTMX / Pico.css 走 jsdelivr CDN**:浏览器需能访问 `cdn.jsdelivr.net`;内网部署请把这两份资产 vendor 到 `app/static/`。
    ```

    关键约束(D-78/79 + INFRA-10):
    - 单文件 README,无 `docs/ACCEPTANCE.md` / `docs/DEPLOY.md`(D-79)
    - ~150 行(不机械数行,但不超过 200 行)
    - 资深运维向:env 速查表 + 1 个 docker run 完整示例 + 1 个 nginx server snippet + smoke 9 步清单
    - **不**逐条解释 env vars 含义(只列表 + 一句生成方式)
    - **不**给手把手 docker desktop 截图
    - **不**列长串故障排查 FAQ(只列 4 条 CD-38 限定的)
    - 验收清单 9 步(D-85)纯人类可执行,无 BDD 框架(CD-40)
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md && grep -E '^# 互动百科' README.md && grep -E '^## 本地开发' README.md && grep -E '^## 生产部署' README.md && grep -E '^## nginx' README.md && grep -E '^## 测试' README.md && grep -E '^## 验收清单' README.md && grep -E '^## 已知限制' README.md && grep -cE '^[0-9]+\. ' README.md  # 9 步验收清单</automated>
  </verify>
  <done>
    - `README.md` 存在,~150 行(实际 130-200 之间)
    - 含 6 个二级标题:项目简介 / 本地开发 / 生产部署 / 测试 / 验收清单 / 已知限制
    - 含 env vars 速查表(6 行)
    - 含 docker run 完整示例
    - 含 nginx server block snippet(完整 30 行)
    - 含 9 步 smoke 验收清单(纯 markdown 数字列表)
    - 含 4 条 FAQ
  </done>
</task>

<verification>
完成 Plan 5.2 后,做以下静态 + 测试验证:

1. **requirements.txt 完整性** (D-87):
   - `grep -c '^' requirements.txt` 输出 10
   - `grep -E '^gunicorn>=21' requirements.txt` 退出 0(Plan 5.1 留的)
   - `grep -E '^pytest>=8' requirements.txt` 退出 0
   - `grep -E '^pytest-flask>=1\.3' requirements.txt` 退出 0

2. **tests/conftest.py 形态** (CD-25/26/35):
   - `test -f tests/conftest.py`
   - `test -f tests/__init__.py`
   - `grep -E 'from app import app' tests/conftest.py` 退出 0
   - `grep -E "SQLALCHEMY_DATABASE_URI.*sqlite" tests/conftest.py` 退出 0
   - `grep -E "WTF_CSRF_ENABLED.*False" tests/conftest.py` 退出 0

3. **tests/test_smoke.py 形态** (D-85):
   - `test -f tests/test_smoke.py`
   - `grep -E 'def test_full_user_flow' tests/test_smoke.py` 退出 0
   - `grep -cE "client\.(post|get)\('/api/" tests/test_smoke.py` ≥ 7(8 个 api + logout 算 9 步)

4. **tests/test_admin.py 形态** (D-86):
   - `test -f tests/test_admin.py`
   - `grep -E 'def test_admin_can_delete_any' tests/test_admin.py` 退出 0
   - `grep -E 'def test_non_admin_forbidden' tests/test_admin.py` 退出 0
   - `grep -E 'assert.*status_code == 403' tests/test_admin.py` 退出 0

5. **Makefile 形态** (CD-30/32):
   - `test -f Makefile`
   - `grep -E '^test:' Makefile` 退出 0
   - `grep -E '^smoke:' Makefile` 退出 0
   - `grep -E '^docker-build:' Makefile` 退出 0
   - `grep -E '^docker-run:' Makefile` 退出 0

6. **README.md 形态** (D-78/79 + INFRA-10 + TEST-05):
   - `wc -l README.md` 在 130-200 之间
   - `grep -E '^# 互动百科' README.md` 退出 0
   - `grep -E '^## 本地开发' README.md` 退出 0
   - `grep -E '^## 生产部署' README.md` 退出 0
   - `grep -E '^## 测试' README.md` 退出 0
   - `grep -E '^## 验收清单' README.md` 退出 0
   - `grep -E '^## 已知限制' README.md` 退出 0
   - 验收清单至少 9 个 `^[0-9]+\. ` 行

7. **端到端 make test**:
   - `make test` 退出码 0
   - 输出含 `test_full_user_flow PASSED` + `test_admin_can_delete_any PASSED` + `test_non_admin_forbidden PASSED`

8. **Makefile smoke 单跑**:
   - `make smoke` 退出码 0
   - 只跑 `tests/test_smoke.py`(不跑 admin)

**Plan 5.2 实施完成后,Phase 5 全部 6 条 SUCCESS 标准可 verified(2 个 plan 共同)。**
</verification>

<success_criteria>
- INFRA-10, TEST-01..05 六条需求对应的 README + tests + Makefile 文件就位
- `make test` 全绿(9 步 smoke + admin 删 + 非 admin 403)
- README 第三方可独立 `docker compose up -d` + 9 步验收清单走完
- 现有 Phase 1-4 代码 0 重构(Plan 5.2 全为新增文件 + requirements.txt 2 行增量)
- 6 个任务 verify 命令全过
</success_criteria>

<output>
完成 Plan 5.2 全部 6 个任务后,创建 `.planning/phases/05-docker-deployment-tests-acceptance/05-02-SUMMARY.md`(由 execute-phase 流程生成)。Phase 5 全部 2 个 plan 执行完成后,生成 `05-VERIFICATION.md`(phase closeout)。
</output>
